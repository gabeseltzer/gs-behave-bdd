---
milestone: v1.4.0
milestone_name: Deprecate featuresPath & Notification Suppression
audited: 2026-05-04T00:00:00Z
status: tech_debt
scores:
  requirements: 15/15
  phases: 2/3 (phase-level artifacts)
  integration: covered_by_phase_17
  flows: 7/7 (migrations integration suite)
gaps:
  requirements: []
  integration: []
  flows: []
  artifact_gaps:
    - phase: 16-deprecate-featurespath
      missing: "16-SUMMARY.md (phase-level)"
      severity: low
      note: "16-VERIFICATION.md exists and is `passed`; per-plan SUMMARYs (16-01..16-06) cover all work. Phase artifact, not a code gap."
    - phase: 17-cross-cutting-verification
      missing: "17-SUMMARY.md and 17-VERIFICATION.md (phase-level)"
      severity: low
      note: "Phase 17 is itself the cross-cutting verification phase — its three plan summaries (17-01, 17-02, 17-03) document the migrations integration suite, registration in runTestSuites.ts, the monorepo-scan regression fix, and Phase 15 HUMAN-UAT closeout. No code gap; missing phase-level rollup artifacts only."
tech_debt:
  - phase: 15-notification-suppression
    items:
      - "test/unit/vscode.mock.ts:171-173 — dead legacy `suppressMultiConfigNotification` fallback in mock get(); unreachable (migration uses inspect()). One-line cleanup."
  - phase: 17-cross-cutting-verification
    items:
      - "src/common.ts hasFeaturesFolder() — Phase 12 `activeProjectCache` is not invalidated when `discoveryDepth` changes. Worked around by reading current depth at lookup time (commit c08ced5); lesson recorded but cache-invalidation strategy still ad-hoc."
      - "Multiroot integration suite hits an environmental mutex flake (`Another instance of app 'Code' is already active`) when developer's own VS Code is running. Not a regression; document or isolate in CI."
  - phase: milestone-wide
    items:
      - "package.json version still 1.3.0 — needs bump to 1.4.0 as part of /gsd-complete-milestone."
nyquist:
  compliant_phases: 3
  partial_phases: 0
  missing_phases: 0
  overall: compliant
  detail: "Phase 15, 16, 17 VALIDATION.md files all carry `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true` (validated 2026-05-04 via commits 4c5bffe / 6f32765 / 1599ec2). Coverage is explicit: 696-passing unit suite plus the new migrations integration suite registered in runTestSuites.ts."
---

# v1.4.0 Milestone Audit — Deprecate featuresPath & Notification Suppression

**Status:** `tech_debt` — all 15 requirements satisfied, no critical blockers, Nyquist sign-off complete across all three phases. Remaining debt is artifact-rollup gaps (16/17 phase-level SUMMARY) and small code cleanups; review before completing the milestone.

## Scope

Phases 15, 16, 17 — milestone defined in `.planning/ROADMAP.md`. Definition of done aggregated from per-phase success criteria: reusable suppression infrastructure (Phase 15), `featuresPath` deprecation with auto-migration (Phase 16), and end-to-end regression pass across both (Phase 17).

## Requirements Coverage (15/15)

3-source cross-reference: REQUIREMENTS.md traceability ↔ VERIFICATION.md tables ↔ SUMMARY frontmatter `requirements-completed`.

### NOTIF-* (Phase 15) — 8/8 satisfied

| ID | Description | Verification | Phase 15 SUMMARY frontmatter | REQUIREMENTS.md | Final |
|----|-------------|--------------|------------------------------|-----------------|-------|
| NOTIF-01 | `suppressedNotifications` array setting in package.json | VERIFIED | listed | `[x]` | satisfied |
| NOTIF-02 | Reusable suppression module | VERIFIED | listed | `[x]` | satisfied |
| NOTIF-03 | DSA writes to WorkspaceFolder scope | VERIFIED | listed | `[x]` | satisfied |
| NOTIF-04 | Multi-config notification uses wrapper | VERIFIED | listed | `[x]` | satisfied |
| NOTIF-05 | Legacy boolean schema entry removed | VERIFIED | listed | `[x]` | satisfied |
| NOTIF-06 | Legacy boolean auto-migrated to array | VERIFIED | listed | `[x]` | satisfied |
| NOTIF-07 | Unit tests for suppression module | VERIFIED | listed | `[x]` | satisfied |
| NOTIF-08 | TestWorkspaceConfig mock updated | VERIFIED | listed | `[x]` | satisfied |

Phase 15 verifier reported `human_needed` at the time pending two Extension-Dev-Host smoke checks; both **closed by Phase 17 automation** (Tests 5 and 7 in `test/integration/migrations suite/extension.test.ts`). 15-HUMAN-UAT.md updated to `status: complete`, both items `passed`.

### DEP-* (Phase 16) — 7/7 satisfied

| ID | Description | Verification | Phase 16 SUMMARY frontmatter | REQUIREMENTS.md | Final |
|----|-------------|--------------|------------------------------|-----------------|-------|
| DEP-01 | `featuresPath` removed from schema | VERIFIED | n/a (no phase-level SUMMARY) | `[x]` | satisfied |
| DEP-02 | Auto-migrate explicit value at any scope | VERIFIED | n/a | `[x]` | satisfied |
| DEP-03 | Migration writes to same scope | VERIFIED | n/a | `[x]` | satisfied |
| DEP-04 | Post-migration notification shown | VERIFIED | n/a | `[x]` | satisfied |
| DEP-05 | Internal code reads only `featuresPaths[]` | VERIFIED | n/a | `[x]` | satisfied |
| DEP-06 | testWorkspaceConfig mock updated | VERIFIED | n/a | `[x]` | satisfied |
| DEP-07 | Unit tests cover migration edge cases | VERIFIED | n/a | `[x]` | satisfied |

Phase 16 lacks a phase-level `16-SUMMARY.md` rollup, but `16-VERIFICATION.md` is `passed` with all 7 DEP requirements explicitly traced to evidence in source code. Per-plan summaries (16-01..16-06) carry the `requirements-completed` frontmatter.

**Orphan check:** No requirements present in REQUIREMENTS.md traceability table that are absent from phase verifications. Zero orphans.

## Phase Verification Status

| Phase | VERIFICATION.md | SUMMARY.md | Status | Notes |
|-------|-----------------|-----------|--------|-------|
| 15-notification-suppression | ✓ | ✓ | `human_needed` → effectively `passed` | UAT items closed by Phase 17 automation per 15-HUMAN-UAT.md update |
| 16-deprecate-featurespath | ✓ (`passed`) | ✗ | passed | Missing phase-level SUMMARY rollup |
| 17-cross-cutting-verification | ✗ | ✗ | de facto passed | Three plan-level summaries cover all work; npm test green (696 unit + migrations integration suite) |

## Cross-Phase Integration

**Integration checker not separately spawned** — Phase 17 IS the cross-cutting integration verification phase, completed 2026-04-30. It produced:

- `test/integration/migrations suite/extension.test.ts` — 7 real-VSCode tests covering A1 probe contract, scope-preserving migration, DSA flow, and notification suppression behavior.
- Registration in `test/integration/runTestSuites.ts` (commit 27e5af3) — runs against `migration-stale` fixture in npm test pipeline.
- Regression bisect + fix for Phase 12 stale `activeProjectCache` interaction with `discoveryDepth=0` (`src/common.ts`, commit c08ced5). Documented in `.planning/debug/resolved/monorepo-scan-discoverydepth0-flake.md`.

**Wiring verified end-to-end:**
- `extension.ts:307-310` activation loop runs `migrateLegacyFeaturesPath` then `migrateLegacySuppressMultiConfig` then `config.reloadSettings` (D-18 ordering).
- Both migrations delegate to the shared `migrateScopedSetting<TSrc, TDest>` primitive in `src/notifications.ts`.
- `showSuppressibleNotification` is the single rendering path for both `multiConfigNotification` and `featuresPathMigration` keys.

## End-to-End Flows

All exercised by the new migrations integration suite running in real VS Code:

| Flow | Source | Coverage |
|------|--------|----------|
| Fresh activation, no deprecated settings | migrations suite | passing |
| Activation with old `featuresPath` only | migrations suite | passing — migrated, scope-preserved |
| Activation with old `suppressMultiConfigNotification: true` only | migrations suite | passing — migrated, scope-preserved, no UI |
| Activation with both legacy keys | migrations suite | passing — both migrate, ordering D-18 honored |
| Migration notification shown after `featuresPath` migration | migrations suite | passing |
| DSA click on migration notification suppresses key at WorkspaceFolder | migrations suite Test 5 | passing |
| `cfg.inspect()` returns scope values for unregistered key (A1 probe contract) | migrations suite Test 7 | passing |

## Tech Debt Items

**Phase 15:**
- `test/unit/vscode.mock.ts:171-173` — dead legacy fallback branch (Finding 1 from 15-VERIFICATION). Unreachable, cosmetic. One-line cleanup.

**Phase 17:**
- `src/common.ts hasFeaturesFolder()` — `activeProjectCache` is read-time-checked against current `discoveryDepth` rather than invalidated on settings change. Working but ad-hoc. Recommended follow-up: pair `clearScanResultCache()` with project-list invalidation when discovery-influencing settings change (lesson recorded in 17-03-SUMMARY).
- Multiroot integration suite has an environmental mutex flake on developer machines when their own VS Code is running. Not a regression. Document in contributor guide or isolate to CI.

**Milestone-wide:**
- `package.json` version is still `1.3.0`. Bump to `1.4.0` is part of `/gsd-complete-milestone`.
- Phase artifact gaps: `16-SUMMARY.md` and `17-SUMMARY.md`/`17-VERIFICATION.md` missing. Code is verified; rollup documents are not.

## Nyquist Coverage

| Phase | VALIDATION.md | `nyquist_compliant` | `status` | Action |
|-------|---------------|---------------------|----------|--------|
| 15 | exists | true | complete | none — signed off 2026-05-04 |
| 16 | exists | true | complete | none — signed off 2026-05-04 |
| 17 | exists | true | complete | none — signed off 2026-05-04 |

Coverage is explicit: all 23 mapped tasks in Phase 15 land in the green unit suite (696 passing); Phase 16 reconstructed and validated via per-plan SUMMARYs and `16-VERIFICATION.md`; Phase 17 Wave 0 deliverables shipped with the new migrations integration suite registered in `runTestSuites.ts`.

---

## ⚡ Milestone v1.4.0 — Tech Debt Review

**Score:** 15/15 requirements satisfied
**Report:** `.planning/v1.4.0-MILESTONE-AUDIT.md`

All 15 requirements (NOTIF-01..08, DEP-01..07) are satisfied with code-level evidence. No critical blockers. No broken flows. Cross-phase integration verified through Phase 17's migrations integration suite (7 tests, real VS Code, all passing). Accumulated debt is low-severity and consists of artifact-completion and Nyquist sign-off rather than code gaps.

### Tech Debt by Phase

**Phase 15:**
- `test/unit/vscode.mock.ts:171-173` legacy mock fallback (dead code)

**Phase 17 / Milestone-wide:**
- `src/common.ts` ad-hoc cache-invalidation pattern for `activeProjectCache` vs `discoveryDepth`
- Multiroot integration suite environmental mutex flake (documented, not a regression)
- `package.json` version still 1.3.0 — bump on milestone close
- 16-SUMMARY.md, 17-SUMMARY.md, 17-VERIFICATION.md phase-level rollups absent

### Total: 5 items across 3 phases

───────────────────────────────────────────────────────────────

## ▶ Options

**A. Complete milestone** — accept debt, track in backlog

```
/gsd-complete-milestone v1.4.0
```

**B. Close the artifact + Nyquist gaps first** — add a small closure phase to (1) write the missing phase-level SUMMARY/VERIFICATION rollups, (2) run `/gsd-validate-phase` for 15/16/17, and (3) clean the `vscode.mock.ts` dead branch. Use the standard chain:

```
/clear then:
/gsd-phase --insert 18 "Address v1.4.0 tech debt: artifact rollups, Nyquist sign-off, mock cleanup"
/gsd-discuss-phase 18
/gsd-plan-phase 18
/gsd-execute-phase 18
```

For Nyquist-only sign-off without a new phase, prefer running:

```
/gsd-validate-phase 15
/gsd-validate-phase 16
/gsd-validate-phase 17
```

───────────────────────────────────────────────────────────────
