# Milestone v1.1: Config File Watching

**Status:** SHIPPED 2026-04-17
**Phases:** 4-6 (3 phases)
**Total Plans:** 9

## Overview

v1.1 made the auto-discovery behavior from v1.0 reactive: the extension now watches all 5 behave config formats, silently rebuilds the test tree on config edits, and guards test execution when a workspace's config is malformed. End-to-end coverage was added as the 14th integration suite, locked in behind a 3-run flakiness gate.

## Phases

### Phase 4: Watcher & Run Guard

**Goal:** Users see the test tree update automatically when a behave config file changes, and are warned before running tests against a workspace with a malformed config
**Depends on:** Phase 3 (v1.0)
**Requirements:** WATCH-01, WATCH-02, WATCH-03, WATCH-04, WATCH-05, WATCH-06, GUARD-01, GUARD-02, GUARD-03, GUARD-04, TEST-07, TEST-09
**Plans:** 2 plans

Plans:

- [x] 04-01-PLAN.md — Config file watcher with debounce + extension.ts wiring + watcher unit tests
- [x] 04-02-PLAN.md — Run guard in testRunHandler + run guard unit tests

**Details:**

- Per-workspace FileSystemWatcher over brace-glob `{behave.ini,.behaverc,setup.cfg,tox.ini,pyproject.toml}` with 500ms debounce, routed through `configurationChangedHandler(undefined, undefined, true)`
- `wkspConfigWatchers: Map<vscode.Uri, vscode.FileSystemWatcher[]>` parallel to `wkspWatchers`; disposed and recreated on workspace folder changes
- `checkRunGuard` reads `configError` from `getDiscoveryEntry()` (not `WorkspaceSettings` snapshot); scoped to queued-test workspaces only
- Guard is non-blocking (Run Anyway / Open Config File / Cancel) and fires for both runs and debug sessions via the shared runHandler
- 10 watcher unit tests (sinon fake timers) + 8 run-guard unit tests; 539 unit tests pass

### Phase 5: Integration Verification

**Goal:** The watcher + run-guard behavior is verified end-to-end through an automated integration test so regressions are caught at CI time
**Depends on:** Phase 4
**Requirements:** TEST-08
**Gap Closure:** Closes TEST-08 and the `config-edit → test-tree-update` flow gap from v1.1-MILESTONE-AUDIT.md; automated coverage obsoletes the 5 Phase 4 human UAT items
**Plans:** 5 plans

Plans:

- [x] 05-01-PLAN.md — Fixture scaffolding: `example-projects/watcher-integration/` with `behave.ini` + `features/` + `features-alt/`
- [x] 05-02-PLAN.md — Shared poll helper: `test/integration/suite-shared/waitForTestTree.ts`
- [x] 05-03-PLAN.md — Watcher integration suite: delete / create / change tests
- [x] 05-04-PLAN.md — Run-guard integration suite: three-branch tests against real malformed-config cache entry (D-16 precedence trick)
- [x] 05-05-PLAN.md — Register 14th suite in `runTestSuites.ts` + three-runs flakiness gate + close TEST-08

**Details:**

- Dedicated fixture directory owned by Phase 5 tests, preventing cross-suite pollution (D-05)
- `waitForTestTree` replaces wall-clock sleeps with deterministic state gating — handles Windows FileSystemWatcher 1-5s delete latency (≥15000ms timeouts)
- Suites that mutate `behave.ini` snapshot + restore in `suiteSetup`/`suiteTeardown` — per-test `finally` blocks are no-op when D-08 chain sequencing applies
- 3-run green pass on Windows satisfied the D-21 flakiness gate

### Phase 6: v1.1 Tech Debt & Admin Cleanup

**Goal:** Close code-review findings and planning hygiene issues identified by the v1.1 milestone audit so v1.1 ships clean
**Depends on:** Phase 4 (code fixes target Phase 4 source files)
**Requirements:** None (tech debt + admin hygiene, no new REQ-IDs)
**Gap Closure:** Closes audit `tech_debt` section — WR-01, WR-02, IN-01, IN-02 code-review findings + 2 admin hygiene items
**Plans:** 2 plans

Plans:

- [x] 06-01-PLAN.md — Code fixes: WR-01, WR-02, IN-01, IN-02 in testRunHandler.ts + configWatcher.ts
- [x] 06-02-PLAN.md — Admin: `requirements_completed` frontmatter in 04-02-SUMMARY + REQUIREMENTS.md traceability updates

**Details:**

- `testRunHandler.ts`: `completed` diagLog moved into `finally`, `==` → `===`, stray `}` in template literal removed
- `configWatcher.ts`: `configDebounceTimers` Map key normalized via `uriId(wkspUri)` (drive-letter-casing safe)
- REQUIREMENTS.md traceability: 12 Phase 4 rows flipped to `[x]`/Complete; TEST-08 was already Complete from Phase 5

---

## Milestone Summary

**Decimal Phases:** None — Phases 4, 5, 6 all sequential.

**Key Decisions:**

- Config watcher callback routes through `configurationChangedHandler(undefined, undefined, true)` — preserves integration test guard, log clearing, watcher rebuild, and `clearNotifiedErrors=true`
- Brace-expansion glob `{behave.ini,.behaverc,setup.cfg,tox.ini,pyproject.toml}` — bare filenames silently fail (VS Code bug #164925)
- 500ms debounce mandatory — stale file read race on `onDidChange` (VS Code bug #72831)
- Run guard reads discovery cache via `getDiscoveryEntry()`, NOT from `WorkspaceSettings` snapshot (staleness-safe)
- Non-blocking guard UX: user can always proceed ("Run Anyway"); hard blocking was explicitly out of scope
- Dedicated test fixtures (D-05): suites that mutate fs state get their own `example-projects/` directory
- `waitForTestTree` predicate polling (D-08) replaces wall-clock sleeps across integration suites
- Phase 5 automation obsoleted 5 Phase 4 human UAT items; audit `gaps_found` status was cleared by Phase 5 + Phase 6 execution

**Issues Resolved:**

- v1.1-MILESTONE-AUDIT `gaps_found` → resolved: TEST-08 closed (Phase 5), tech-debt + admin hygiene closed (Phase 6)
- Windows FileSystemWatcher delete latency (1-5s) — handled in Phase 5 fix `b54de65` via ≥15000ms timeouts
- Pitfall 14 confirmed: `configWatcher` bypasses `configurationChangedHandler` so `integrationTestRun = true` does not prevent watcher from firing in tests

**Issues Deferred:**

- Subdirectory config scanning (DISC-07, depth-3) → v1.2 candidate
- Multiple feature paths via `featuresUris[]` (DISC-08) → v1.2 candidate
- Multiple behave projects per workspace (MULTI-01) + `Behave BDD: Select Project` quick-pick (MULTI-02) → Milestone 3

**Technical Debt Incurred:**

- None — Phase 6 cleared all outstanding code-review findings and admin hygiene before milestone close

---

*For current project status, see .planning/ROADMAP.md*
