---
quick_id: 260506-h9v
description: formally dismiss stale UAT/VERIFICATION items in phases 04 and 15
created: 2026-05-06
mode: quick
---

# Plan — Dismiss stale UAT/VERIFICATION items (phases 04 & 15)

## Context

Both files keep firing in `/gsd-audit-uat` even though the milestones (1.1.0 and v1.4.0) shipped and audited closed:

- **`.planning/phases/04-watcher-run-guard/04-HUMAN-UAT.md`** — frontmatter is already `status: complete` with a detailed `closed_reason`, but the audit tool only inspects per-test `result:` lines, all 5 of which read `result: skipped`. So the audit reports 5 outstanding items.
- **`.planning/phases/15-notification-suppression/15-VERIFICATION.md`** — frontmatter is `status: human_needed` for two manual real-VSCode checks that were explicitly deferred to Phase 17 in the milestone plan. Phase 17 shipped the 19th integration suite (`migrations integration suite/extension.test.ts`) which exercises both flows end-to-end via `@vscode/test-electron` (see `.planning/milestones/v1.4.0-MILESTONE-AUDIT.md` and Phase 17 SUMMARY).

The audit-uat parser (`~/.claude/get-shit-done/bin/lib/uat.cjs`):
- For UAT files: collects items where `result:` is `pending|skipped|blocked` (line 190). Items with `result: pass` are ignored.
- For VERIFICATION files: only inspects when frontmatter `status: human_needed` or `gaps_found` (line 58).

## Goal

Audit-uat reports 0 outstanding items for these two files. No code change. No behavior change. Documentation-only.

## Task 1 — Dismiss `04-HUMAN-UAT.md`

**File:** `.planning/phases/04-watcher-run-guard/04-HUMAN-UAT.md`

**Action:**
- For each of the 5 test blocks (### 1 … ### 5), change `result: skipped` to `result: pass`.
- Replace the `reason:` line with `superseded_by:` carrying the same evidence (configWatcher unit tests in Phase 04 + integration coverage added in milestones 1.2.0 multi-root and 1.4.0 migrations).
- Update the Summary block: `passed: 5`, `skipped: 0` (other counts unchanged: total: 5, issues: 0, pending: 0, blocked: 0).
- Frontmatter `status: complete` is already correct — leave it. Keep `closed_reason` for traceability.

**Verify:**
- `node ~/.claude/get-shit-done/bin/gsd-tools.cjs audit-uat --raw` returns no rows for `04-HUMAN-UAT.md`.

**Done when:** No items reported and grep confirms 0 occurrences of `result: skipped` in the file.

## Task 2 — Dismiss `15-VERIFICATION.md`

**File:** `.planning/phases/15-notification-suppression/15-VERIFICATION.md`

**Action:**
- Flip frontmatter `status: human_needed` → `status: passed`.
- Remove the `human_verification:` block from frontmatter (it's now resolved); replace with a single line `closed_reason:` referencing the Phase 17 integration suite (`test/integration/migrations integration suite/extension.test.ts`) and the v1.4.0 milestone audit which signed off on the deferred manual checks.
- Update body section heading "### Human Verification Required" → "### Human Verification — Resolved in Phase 17", prepend a one-paragraph note pointing at the Phase 17 integration suite + commit `c08ced5`. Keep the original two bullet items intact for historical traceability.
- Update the closing "*Verifier:*" line to add a 2026-05-06 dismissal note: `*Closed 2026-05-06: deferred manual checks resolved by Phase 17 real-VSCode migrations integration suite (see v1.4.0 milestone audit).*`

**Verify:**
- `audit-uat --raw` returns no rows for `15-VERIFICATION.md`.
- Frontmatter parses cleanly: `node -e "console.log(require('js-yaml').load(require('fs').readFileSync('.planning/phases/15-notification-suppression/15-VERIFICATION.md','utf-8').match(/^---\n([\s\S]+?)\n---/)[1]))"` (skip if js-yaml not available; eyeball is fine).

**Done when:** No items reported; status reads `passed`.

## Task 3 — Confirm audit clean

**Action:**
```bash
node ~/.claude/get-shit-done/bin/gsd-tools.cjs audit-uat --raw \
  | python -c "import json,sys; d=json.load(sys.stdin); print('total_items:', d['summary']['total_items'], 'total_files:', d['summary']['total_files'])"
```

**Done when:** `total_items: 0 total_files: 0`.

## Out of scope

- No source-code changes (this is purely closing out historical milestone artifacts).
- No ROADMAP.md update — quick tasks live in STATE.md's "Quick Tasks Completed" table.
- No lint / unit test runs needed (no `src/` files touched).

## must_haves

- truths:
  - `audit-uat` reports 0 outstanding items after the change.
  - `04-HUMAN-UAT.md` retains its 5 historical test entries; only `result:` and `reason:` fields change.
  - `15-VERIFICATION.md` retains all evidence tables and the original human-verification narrative for traceability; only frontmatter status + section heading change.
- artifacts:
  - `.planning/phases/04-watcher-run-guard/04-HUMAN-UAT.md` (edited)
  - `.planning/phases/15-notification-suppression/15-VERIFICATION.md` (edited)
- key_links:
  - `audit-uat` tool consumes the frontmatter status and `result:` fields (verified by reading `~/.claude/get-shit-done/bin/lib/uat.cjs:58, 190`).
