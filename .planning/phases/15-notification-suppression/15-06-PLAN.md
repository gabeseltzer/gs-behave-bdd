---
phase: 15-notification-suppression
plan: 06
type: execute
wave: 5
depends_on: ["15-05"]
files_modified: []
autonomous: true
requirements: [NOTIF-07]
tags: [verification, lint, unit-tests, integration-smoke, phase-gate]
must_haves:
  truths:
    - "`npm run test:unit` exits 0 — full unit suite green with no regressions vs. the pre-phase baseline"
    - "`npx eslint src --ext ts` exits 0 with no output"
    - "`npx tsc --noEmit -p .` exits 0"
    - "All NOTIF-07 sub-checks pass: `npm run test:unit -- --grep \"notifications\"` and the schema and ordering and migration sub-suites"
    - "Webpack bundle compiles cleanly: `npm run compile` exits 0"
    - "Optional: integration smoke test for activation flow runs (manual or via npm run test:integration if feasible)"
    - "Phase Summary written to 15-SUMMARY.md (phase-level summary; aggregates the per-plan summaries)"
  artifacts:
    - path: ".planning/phases/15-notification-suppression/15-SUMMARY.md"
      provides: "Phase-level summary aggregating per-plan outcomes"
  key_links:
    - from: "all NOTIF-* requirements"
      to: ".planning/phases/15-notification-suppression/15-VALIDATION.md"
      via: "every row's `<automated>` command verified green"
      pattern: "passing"
---

<objective>
Phase-level verification gate: confirm every requirement (NOTIF-01..NOTIF-08) is
GREEN by running the full sampling battery defined in 15-VALIDATION.md, plus
lint, typecheck, full unit suite, and webpack compile. No code edits in this
plan — verification only.

Purpose: Single source of truth that Phase 15 is complete and ready for the
phase-level checker. Any failure here surfaces a defect that the corresponding
implementation plan must fix (revision mode).
Output: 15-SUMMARY.md (phase-level summary). All verify commands pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-notification-suppression/15-CONTEXT.md
@.planning/phases/15-notification-suppression/15-RESEARCH.md
@.planning/phases/15-notification-suppression/15-VALIDATION.md
@.planning/phases/15-notification-suppression/15-01-SUMMARY.md
@.planning/phases/15-notification-suppression/15-02-SUMMARY.md
@.planning/phases/15-notification-suppression/15-03-SUMMARY.md
@.planning/phases/15-notification-suppression/15-04-SUMMARY.md
@.planning/phases/15-notification-suppression/15-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run lint, typecheck, full unit suite, and webpack compile</name>
  <files>(no edits — verification only)</files>
  <read_first>
    - .planning/phases/15-notification-suppression/15-VALIDATION.md (every row in the per-task verification map; sampling rate section)
    - CLAUDE.md ("After Every Code Change", "Unit Tests")
    - package.json (npm scripts — confirm `compile`, `test:unit` names match)
  </read_first>
  <behavior>
    Run, in order:
      1. `npx eslint src --ext ts` — must exit 0 with NO output
      2. `npx tsc --noEmit -p .` — must exit 0
      3. `npm run test:unit` — must exit 0 (full suite green)
      4. `npm run compile` (or whichever script invokes webpack) — must exit 0
      5. Targeted sub-suite samples (catch-all for VALIDATION.md rows):
         - `npm run test:unit -- --grep "notifications"` — covers NOTIF-02..04, NOTIF-06, NOTIF-07, activation ordering
         - `npm run test:unit -- --grep "package.json schema"` — covers NOTIF-01, NOTIF-05
         - `npm run test:unit -- --grep "settings"` — covers NOTIF-08 cascade
      6. Schema-shape inline checks (the same `node -e` snippets from VALIDATION.md):
         - `node -e "const p=require('./package.json'); const s=p.contributes.configuration.properties['gs-behave-bdd.suppressedNotifications']; if(s.type!=='array'||s.items.type!=='string'||!Array.isArray(s.default)||s.default.length!==0) process.exit(1)"`
         - `node -e "const p=require('./package.json'); if('gs-behave-bdd.suppressMultiConfigNotification' in p.contributes.configuration.properties) process.exit(1)"`
      7. Code hygiene grep (legacy references should be confined):
         - `grep -rn "suppressMultiConfigNotification" src/` — only matches in `src/notifications.ts`
         - `grep -rn "suppressMultiConfigNotification" test/unit/` — only matches in `test/unit/notifications.test.ts` and `test/unit/packageJsonSchema.test.ts`

    If any step fails: STOP. Identify the failing requirement (use the per-task
    verification map in 15-VALIDATION.md to map the failure to a NOTIF-* ID).
    Document the failure in `.planning/phases/15-notification-suppression/15-06-FAILURE.md`
    and surface to the orchestrator for revision-mode planning.
  </behavior>
  <action>
    Execute the verification battery above. Capture stdout/stderr to a log if
    helpful. After all commands complete:
      - If ALL pass: proceed to Task 2 (write phase summary).
      - If ANY fails: write FAILURE.md with command, exit code, and failure
        excerpt; STOP and surface to orchestrator. Do not proceed to Task 2.
  </action>
  <verify>
    <automated>npx eslint src --ext ts && npx tsc --noEmit -p . && npm run test:unit && npm run compile && node -e "const p=require('./package.json'); const s=p.contributes.configuration.properties['gs-behave-bdd.suppressedNotifications']; if(s.type!=='array'||s.items.type!=='string'||!Array.isArray(s.default)||s.default.length!==0){process.exit(1)} if('gs-behave-bdd.suppressMultiConfigNotification' in p.contributes.configuration.properties){process.exit(2)} console.log('schema ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src --ext ts` exit code 0, no output
    - `npx tsc --noEmit -p .` exit code 0
    - `npm run test:unit` exit code 0; mocha summary line shows "passing" with NO "failing"
    - `npm run compile` exit code 0; webpack output shows no errors
    - `npm run test:unit -- --grep "notifications"` exit code 0; at least 20 tests pass (rough target — combined Phase 15 suites: A1 probe + isSuppressed + suppressNotification + showSuppressibleNotification + migration + activation ordering + extension wiring)
    - `npm run test:unit -- --grep "package.json schema"` exit code 0
    - `npm run test:unit -- --grep "settings"` exit code 0
    - `node -e "..."` schema-shape and absence inline checks both exit 0 (combined check in verify command above)
    - `grep -rn "suppressMultiConfigNotification" src/` returns matches ONLY in `src/notifications.ts`
    - `grep -rn "suppressMultiConfigNotification" test/unit/` returns matches ONLY in `test/unit/notifications.test.ts` and `test/unit/packageJsonSchema.test.ts`
  </acceptance_criteria>
  <done>
    Every verification command green. Phase 15 ready for phase-level checker /
    `/gsd-verify-work`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write phase summary 15-SUMMARY.md</name>
  <files>.planning/phases/15-notification-suppression/15-SUMMARY.md</files>
  <read_first>
    - .planning/phases/15-notification-suppression/15-01-SUMMARY.md
    - .planning/phases/15-notification-suppression/15-02-SUMMARY.md
    - .planning/phases/15-notification-suppression/15-03-SUMMARY.md
    - .planning/phases/15-notification-suppression/15-04-SUMMARY.md
    - .planning/phases/15-notification-suppression/15-05-SUMMARY.md
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/phases/15-notification-suppression/15-VALIDATION.md (final state)
  </read_first>
  <behavior>
    Aggregate the five per-plan summaries into a single phase-level summary.
    Document: requirements covered (all 8 NOTIF-*), decisions implemented (all
    11 D-*), pitfalls navigated, files created/modified, follow-ups for Phase 17.
  </behavior>
  <action>
    Use the project's summary template ($HOME/.claude/get-shit-done/templates/summary.md)
    if available. The summary should answer:

      1. WHAT was built — narrative covering the new infrastructure end-to-end:
         schema → wrapper module → migration → wiring → cleanup
      2. WHICH requirements landed — explicit NOTIF-01..NOTIF-08 list with status
      3. WHICH decisions were honored — explicit D-01..D-11 list
      4. WHICH pitfalls/assumptions were navigated — A1 probe outcome, P1-P6 mitigations
      5. WHICH files changed — final list across all 5 plans
      6. WHAT'S DEFERRED — items punted to Phase 17 (e.g., the integration smoke
         test that validates real-VSCode activation with stale settings.json)
      7. PHASE METRICS — total tests added, lines of new code, plans/wave structure
      8. KEY-LINKS verified — all entries from each plan's `must_haves.key_links` confirmed

    Place the file at `.planning/phases/15-notification-suppression/15-SUMMARY.md`.
    Frontmatter should include phase id, milestone, completion date.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-notification-suppression/15-SUMMARY.md && wc -l .planning/phases/15-notification-suppression/15-SUMMARY.md | awk '{if($1<30) exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/15-notification-suppression/15-SUMMARY.md` exists
    - File length >= 30 lines (substantive, not stub)
    - File mentions all 8 NOTIF-* IDs (`grep -c "NOTIF-0" 15-SUMMARY.md` returns >= 8)
    - File mentions all 11 D-* IDs (`grep -c "D-0\|D-1" 15-SUMMARY.md` returns >= 11)
    - File mentions Phase 17 deferral (the real-VSCode activation integration smoke)
    - File frontmatter present (`grep -n "^---" 15-SUMMARY.md` returns 2 matches)
  </acceptance_criteria>
  <done>
    Phase summary written; aggregates plans 01-05; documents follow-ups; ready
    for ROADMAP.md phase-completion update.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification-only plan — no new trust boundaries introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-24 | Repudiation | phase-completion claim without evidence | mitigate | Every NOTIF-* requirement has an automated verification command in 15-VALIDATION.md; this plan runs all of them and writes 15-SUMMARY.md as the audit trail. |
| T-15-25 | Tampering | residual legacy references hiding in unscanned paths | mitigate | Task 1 includes recursive `grep -rn "suppressMultiConfigNotification"` checks across both `src/` and `test/unit/`, with explicit allow-list of acceptable matches (migration helper + migration tests + schema-absence test). |
</threat_model>

<verification>
- `npx eslint src --ext ts && npx tsc --noEmit -p . && npm run test:unit && npm run compile` — all exit 0
- All sub-suite greps GREEN
- Schema-shape and schema-absence inline `node -e` checks PASS
- Legacy-reference grep returns ONLY allow-listed files
- 15-SUMMARY.md exists, frontmatter present, mentions all NOTIF and D IDs
</verification>

<success_criteria>
- NOTIF-07 (composite): all check/suppress/migrate paths green ✓
- All other NOTIF-* (01-06, 08): verified via this plan's grep + run battery ✓
- Lint clean, typecheck clean, full unit suite green, webpack compile green
- Phase 15 SUMMARY documents what was built, what was deferred, what's next
</success_criteria>

<output>
After completion, the phase summary at
`.planning/phases/15-notification-suppression/15-SUMMARY.md` IS the deliverable.
No further per-plan summary needed for this verification-only plan (or write
`15-06-SUMMARY.md` if your project's GSD harness requires per-plan summaries
unconditionally — match the established convention from prior phases).
</output>
