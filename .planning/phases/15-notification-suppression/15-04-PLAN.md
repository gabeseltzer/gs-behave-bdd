---
phase: 15-notification-suppression
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/testWorkspaceConfig.ts
autonomous: true
requirements: [NOTIF-08]
tags: [test-mock, fixtures, configuration, settings]
must_haves:
  truths:
    - "`TestWorkspaceConfig` exposes a `suppressedNotifications` private field with optional `string[]` type"
    - "`TestWorkspaceConfig.get('suppressedNotifications')` returns `[]` when not passed (matches package.json default)"
    - "`TestWorkspaceConfig.get('suppressedNotifications')` returns the passed array when provided"
    - "`TestWorkspaceConfig.inspect('suppressedNotifications')` returns the value via `workspaceFolderValue` (existing mock fidelity)"
    - "Constructor accepts an optional `suppressedNotifications?: string[]` parameter"
    - "Both old and new mock keys coexist briefly (legacy key NOT removed in this plan — Plan 05 cleanup removes the legacy key from `WorkspaceSettings` and `TestWorkspaceConfig`)"
  artifacts:
    - path: "src/testWorkspaceConfig.ts"
      provides: "suppressedNotifications mock field, get/inspect switch cases"
      contains: "suppressedNotifications"
  key_links:
    - from: "src/testWorkspaceConfig.ts::get"
      to: "package.json default `[]`"
      via: "fallback `?? []` in switch case (matches featuresPaths pattern at L90-L91)"
      pattern: "this\\.suppressedNotifications \\?\\? \\[\\]"
---

<objective>
Update `TestWorkspaceConfig` mock to expose the new `suppressedNotifications: string[]`
setting (NOTIF-08 mock-side coverage), so that downstream tests constructing
`TestWorkspaceConfig` can pass the new key when needed.

The four cascading BASE_CFG / makeFakeWkspSettings fixture updates that were
originally scoped to this plan have been FOLDED INTO Plan 01 (per checker
revision: addresses BLOCKER B-2). Plan 01 now atomically lands the strict-undefined
throw on `WorkspaceSettings` AND the four fixture updates that satisfy that throw,
preventing any window where Plan 02 / Plan 03 would observe a red full-suite run
during Wave 2.

This plan now contains a SINGLE TASK: the TestWorkspaceConfig mock surgery.

This plan ADDS the new array support but does NOT remove the legacy
`suppressMultiConfigNotification` mock entries (those are removed in Plan 05/06
cleanup). Keeping both alive in this plan allows Plans 02, 03 to land in parallel
without breaking pre-existing settings tests.

Purpose: TestWorkspaceConfig mock supports the new array shape, ready for any
test that constructs it with `suppressedNotifications: [...]`.
Output: `src/testWorkspaceConfig.ts` carries both old and new fields; switch
cases for the new key in get() and inspect().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-notification-suppression/15-CONTEXT.md
@.planning/phases/15-notification-suppression/15-RESEARCH.md
@.planning/phases/15-notification-suppression/15-PATTERNS.md
@.planning/phases/15-notification-suppression/15-VALIDATION.md
@.planning/phases/15-notification-suppression/15-01-SUMMARY.md
@CLAUDE.md
@AI_INSTRUCTIONS.md

<interfaces>
<!-- Existing patterns the executor MUST mirror exactly. -->

From src/testWorkspaceConfig.ts (current head):
- L17 area: `private featuresPaths: string[] | undefined;` — array field declaration template
- L27 area: `private suppressMultiConfigNotification: boolean | undefined;` — legacy boolean (KEEP in this plan)
- L40 area: constructor signature with `featuresPaths?: string[] | undefined,` — optional array param template
- L57 area: `this.featuresPaths = featuresPaths;` — direct assignment template
- L90-L91 area: `case "featuresPaths": return <T><unknown>(this.featuresPaths ?? []);` — get() switch template (with `?? []` fallback for default)
- L110-L111 area: `case "suppressMultiConfigNotification": return <T><unknown>(this.suppressMultiConfigNotification === undefined ? false : ...);` — legacy boolean get() (KEEP)
- L148-L150 area: `case "featuresPaths": response = <T><unknown>this.featuresPaths; break;` — inspect() switch template (no fallback — undefined returns undefined)
- L175-L176 area: `case "suppressMultiConfigNotification": response = ...; break;` — legacy inspect() (KEEP)
- L185 hardcodes `workspaceFolderValue: response` — fidelity gap (Pitfall 5; see Plan 03 for the workaround)
- L278-L279 area: `case "suppressMultiConfigNotification": ...` in `getExpected()` (KEEP for now; the array key intentionally has no `getExpected` case — featuresPaths precedent)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `suppressedNotifications` to TestWorkspaceConfig mock (preserve legacy entries)</name>
  <files>src/testWorkspaceConfig.ts</files>
  <read_first>
    - src/testWorkspaceConfig.ts (FULL FILE — every line; this is the master mock)
    - .planning/phases/15-notification-suppression/15-PATTERNS.md (`src/testWorkspaceConfig.ts` MODIFY section, lines 355-417)
    - .planning/phases/15-notification-suppression/15-RESEARCH.md (Pitfall 5 — DO NOT modify the workspaceFolderValue-only fidelity at L185)
    - CLAUDE.md (lint mandate)
  </read_first>
  <behavior>
    Add the `suppressedNotifications` mock support, mirroring the `featuresPaths`
    array pattern exactly. Do NOT remove any `suppressMultiConfigNotification`
    references in this plan — both keys coexist until Plan 05/06 cleanup.

    1. ADD `private suppressedNotifications: string[] | undefined;` near the existing
       legacy `private suppressMultiConfigNotification: boolean | undefined;` line.
    2. ADD `suppressedNotifications?: string[] | undefined,` to the constructor's
       parameter destructure list AND to its TypeScript type annotation.
       Place it adjacent to the legacy boolean in BOTH lists.
    3. ADD `this.suppressedNotifications = suppressedNotifications;` in the
       constructor body, near the legacy assignment.
    4. ADD a new case in the `get()` switch (mirror the featuresPaths case at
       L90-L91 — `?? []` fallback because package.json default is `[]`):

       ```typescript
       case "suppressedNotifications":
         return <T><unknown>(this.suppressedNotifications ?? []);
       ```

       Place this adjacent to the legacy `case "suppressMultiConfigNotification"` case.
    5. ADD a new case in the `inspect()` switch (mirror the featuresPaths case at
       L148-L150 — no fallback; undefined returns undefined):

       ```typescript
       case "suppressedNotifications":
         response = <T><unknown>this.suppressedNotifications;
         break;
       ```

       Place this adjacent to the legacy `case "suppressMultiConfigNotification"`.
    6. DO NOT add a `getExpected()` case — featuresPaths has none (precedent;
       PATTERNS.md L357 explicitly says skip).
    7. DO NOT REMOVE any legacy `suppressMultiConfigNotification` field, switch
       case, constructor param, or `getExpected` case — those are removed in
       Plan 05/06 cleanup. Both must coexist for the duration of Wave 2.
  </behavior>
  <action>
    Apply the edits described above. After each edit, the legacy
    `suppressMultiConfigNotification` references MUST still be present (run grep
    to confirm). Then:
      1. `npx eslint src --ext ts` — fix any errors
      2. `npx tsc --noEmit -p .` — confirm types compile
      3. `npm run test:unit -- --grep "settings"` — confirm no settings test broke

    NOTE: The four cascading BASE_CFG / makeFakeWkspSettings fixture updates that
    were originally scoped here as Tasks 2-5 have been folded into Plan 01 (per
    checker BLOCKER B-2 fix). Plan 01 lands the strict-undefined throw and the
    fixture updates atomically, so the full settings suite is green at the moment
    Wave 2 starts. This plan only modifies `src/testWorkspaceConfig.ts`.
  </action>
  <verify>
    <automated>npx eslint src --ext ts && npx tsc --noEmit -p . && grep -c "suppressMultiConfigNotification" src/testWorkspaceConfig.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "private suppressedNotifications" src/testWorkspaceConfig.ts` returns 1 match
    - `grep -n "private suppressMultiConfigNotification" src/testWorkspaceConfig.ts` returns 1 match (legacy preserved)
    - `grep -n 'case "suppressedNotifications"' src/testWorkspaceConfig.ts` returns >= 2 matches (one in get, one in inspect)
    - `grep -n 'case "suppressMultiConfigNotification"' src/testWorkspaceConfig.ts` returns >= 3 matches (legacy preserved in get, inspect, getExpected)
    - `grep -n "this.suppressedNotifications = suppressedNotifications" src/testWorkspaceConfig.ts` returns 1 match
    - `grep -n "this.suppressedNotifications ?? \[\]" src/testWorkspaceConfig.ts` returns 1 match (get() fallback)
    - `npx eslint src --ext ts` exits 0
    - `npx tsc --noEmit -p .` exits 0
    - `npm run test:unit -- --grep "settings"` — full settings suite passes (Plan 01 already updated the four BASE_CFG fixtures; this plan does not touch those files)
  </acceptance_criteria>
  <done>
    `TestWorkspaceConfig` supports both old and new keys. Legacy entries intact
    for downstream cleanup. Full settings suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TestWorkspaceConfig (test-only) → WorkspaceSettings constructor | Test fixtures cross into the real production code path; the mock must satisfy the same strict-undefined-throw constraints as a real VS Code config. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15 | Tampering | TestWorkspaceConfig deviating from package.json schema default | mitigate | Mock `get('suppressedNotifications')` returns `?? []` matching package.json `default: []` (and matching the featuresPaths precedent). Acceptance criteria asserts the fallback. |
| T-15-16 | Spoofing | Test fixtures hiding real-world failure modes | mitigate | Pitfall 5 acknowledged: TestWorkspaceConfig.inspect() only sets workspaceFolderValue. Migration tests in Plan 03 use the per-key inline helper instead — this plan does NOT modify L185 of testWorkspaceConfig.ts. |
</threat_model>

<verification>
- `npx eslint src --ext ts` exits 0
- `npx tsc --noEmit -p .` exits 0
- `npm run test:unit -- --grep "settings"` — all settings tests pass (relies on Plan 01 having landed the four BASE_CFG fixture updates atomically)
</verification>

<success_criteria>
- NOTIF-08 (mock fields): TestWorkspaceConfig has `suppressedNotifications` field + switch cases ✓
- Legacy entries preserved (cleanup is Plan 05/06 — supports parallel Wave 2 execution) ✓
- Single-task plan; no fixture cascade in this plan (folded into Plan 01)
</success_criteria>

<output>
After completion, create `.planning/phases/15-notification-suppression/15-04-SUMMARY.md`.
</output>
