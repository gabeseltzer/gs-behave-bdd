---
phase: 11-ux-polish-regression-hardening
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified: []
autonomous: false
requirements: [TEST-15]

must_haves:
  truths:
    - "Full integration suite passes 3 consecutive runs on Windows with no flaky failures"
    - "No Mocha --retries or .skip used anywhere in new suites"
  artifacts: []
  key_links: []
---

<objective>
Run the 3× Windows CI flakiness gate per TEST-15 / D-11 / D-13 / D-14. The developer runs `npm run test:integration` three times locally on Windows. All three runs must pass with zero failures and zero retries.

Purpose: Match v1.1 precedent (Phase 5 D-21) — confirm no timing-dependent flakiness before milestone close.
Output: Verified 3× green pass.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ux-polish-regression-hardening/11-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 3× Windows CI flakiness gate</name>
  <files></files>
  <action>
Plans 01 and 02 created the integration test fixtures and suites. The full integration suite now includes multi-path, multi-path-settings, and monorepo-scan suites.

Run `npm run test:integration` three separate times on Windows. All three runs must pass with 0 failures.

Per D-13: NO retries (`--retries`), NO `.skip`. If ANY test fails in ANY of the 3 runs, the test must be fixed — not skipped or retried.

Per D-14: This is a one-time manual gate. No automation script needed.

**Pass criteria:**
- Run 1: All suites pass (including new multi-path, multi-path-settings, monorepo-scan)
- Run 2: All suites pass
- Run 3: All suites pass
- Zero flaky tests across all 3 runs
  </action>
  <verify>
    <automated>grep -rn "retries\|\.skip" "test/integration/multi-path suite/" "test/integration/multi-path-settings suite/" "test/integration/monorepo-scan suite/" 2>$null; if ($LASTEXITCODE -ne 0) { echo "No retries or skips found - OK" }</automated>
  </verify>
  <done>Three consecutive `npm run test:integration` runs pass on Windows with zero failures and zero flaky tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan is a manual verification checkpoint.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | No code changes in this plan |
</threat_model>

<verification>
1. Three consecutive `npm run test:integration` runs pass on Windows.
2. No `--retries` or `.skip` in any new test file.
</verification>

<success_criteria>
3× green pass on Windows CI with zero flaky failures — milestone close gate satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ux-polish-regression-hardening/11-03-SUMMARY.md`
</output>
