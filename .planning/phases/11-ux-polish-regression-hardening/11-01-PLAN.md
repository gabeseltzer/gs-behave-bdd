---
phase: 11-ux-polish-regression-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - example-projects/multi-path/behave.ini
  - example-projects/multi-path/features/multi_path_a.feature
  - example-projects/multi-path/features/steps/steps.py
  - example-projects/multi-path/features/environment.py
  - example-projects/multi-path/features-alt/multi_path_b.feature
  - example-projects/multi-path/features-alt/steps/steps.py
  - example-projects/multi-path/features-alt/environment.py
  - example-projects/multi-path-settings/.vscode/settings.json
  - example-projects/multi-path-settings/features/settings_a.feature
  - example-projects/multi-path-settings/features/steps/steps.py
  - example-projects/multi-path-settings/features/environment.py
  - example-projects/multi-path-settings/features-alt/settings_b.feature
  - example-projects/multi-path-settings/features-alt/steps/steps.py
  - example-projects/multi-path-settings/features-alt/environment.py
  - example-projects/monorepo-scan/node_modules/.gitkeep
  - example-projects/monorepo-scan/app-a/behave.ini
  - example-projects/monorepo-scan/app-a/features/app_a.feature
  - example-projects/monorepo-scan/app-a/features/steps/steps.py
  - example-projects/monorepo-scan/app-a/features/environment.py
  - example-projects/monorepo-scan/app-b/behave.ini
  - example-projects/monorepo-scan/app-b/features/app_b.feature
  - example-projects/monorepo-scan/app-b/features/steps/steps.py
  - example-projects/monorepo-scan/app-b/features/environment.py
  - example-projects/monorepo-scan/packages/app-c/behave.ini
  - example-projects/monorepo-scan/packages/app-c/features/app_c.feature
  - example-projects/monorepo-scan/packages/app-c/features/steps/steps.py
  - example-projects/monorepo-scan/packages/app-c/features/environment.py
  - test/unit/settings/logSettingsPlural.test.ts
autonomous: true
requirements: [TEST-14]

must_haves:
  truths:
    - "multi-path/ fixture has behave.ini pointing to single path, with a dormant features-alt/ directory containing different scenario counts"
    - "multi-path-settings/ fixture has .vscode/settings.json with featuresPaths array and no behave.ini"
    - "monorepo-scan/ fixture has three nested behave.ini configs at depths 1 and 2, plus a node_modules/ stub"
    - "logSettings outputs both paths comma-separated when WorkspaceSettings has multiple featuresUris"
  artifacts:
    - path: "example-projects/multi-path/behave.ini"
      provides: "Single-path baseline config for chained edit tests"
      contains: "paths = features"
    - path: "example-projects/multi-path/features/multi_path_a.feature"
      provides: "Primary path with 2 scenarios"
    - path: "example-projects/multi-path/features-alt/multi_path_b.feature"
      provides: "Dormant alt path with 1 scenario"
    - path: "example-projects/multi-path-settings/.vscode/settings.json"
      provides: "featuresPaths array setting"
      contains: "featuresPaths"
    - path: "example-projects/monorepo-scan/app-a/behave.ini"
      provides: "Depth-1 config (primary candidate)"
    - path: "example-projects/monorepo-scan/node_modules/.gitkeep"
      provides: "Marker for scanner exclude-dir assertion"
    - path: "test/unit/settings/logSettingsPlural.test.ts"
      provides: "Unit test for plural logSettings output (SC-5)"
  key_links:
    - from: "example-projects/multi-path/behave.ini"
      to: "example-projects/multi-path/features/"
      via: "paths = features"
      pattern: "paths = features"
    - from: "example-projects/multi-path-settings/.vscode/settings.json"
      to: "example-projects/multi-path-settings/features/"
      via: "featuresPaths array"
      pattern: "featuresPaths.*features.*features-alt"
---

<objective>
Create the three dedicated integration test fixtures (`multi-path/`, `multi-path-settings/`, `monorepo-scan/`) per TEST-14, and add a unit test verifying `logSettings` renders plural `featuresUris` as comma-joined paths per D-09/SC-5.

Purpose: These fixtures are the test substrate for the integration suites in Plan 02. Each fixture exercises a distinct v1.2 discovery path.
Output: Three fixture directories under `example-projects/` and one new unit test file.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-ux-polish-regression-hardening/11-CONTEXT.md

# Reference fixtures — match these patterns exactly
@example-projects/watcher-integration/behave.ini
@example-projects/watcher-integration/features/discovery.feature
@example-projects/watcher-integration/features/steps/steps.py
@example-projects/watcher-integration/features/environment.py
@example-projects/watcher-integration/features-alt/alt.feature
@example-projects/config-only/behave.ini

# logSettings test pattern
@test/unit/settings/verboseLogging.test.ts
@src/settings.ts

<interfaces>
<!-- Key types the executor needs for the logSettings unit test -->

From src/settings.ts:
```typescript
logSettings(logger: Logger, winSettings: WindowSettings) {
  // ...
  wkspEntries.push(["fullFeaturesPaths", this.featuresUris.map(u => u.fsPath).join(", ")]);
  wkspEntries.push(["featuresPaths", this.projectRelativeFeaturesPaths.join(", ")]);
  // ...
}
```

From test/unit/settings/verboseLogging.test.ts (reusable pattern):
```typescript
function makeFakeWkspSettings(presets) {
  return {
    // ... minimal WorkspaceSettings-shaped object
    featuresUris: [vscode.Uri.file('/fake/workspace/features')],
    projectRelativeFeaturesPaths: ['features'],
    // ...
  };
}

function callLogSettings(fakeWksp, verboseLogging): string {
  // Calls WorkspaceSettings.prototype.logSettings.call(fakeWksp, mockLogger, winSettings)
  // Returns logged text
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create multi-path/ and multi-path-settings/ fixtures</name>
  <files>
    example-projects/multi-path/behave.ini
    example-projects/multi-path/features/multi_path_a.feature
    example-projects/multi-path/features/steps/steps.py
    example-projects/multi-path/features/environment.py
    example-projects/multi-path/features-alt/multi_path_b.feature
    example-projects/multi-path/features-alt/steps/steps.py
    example-projects/multi-path/features-alt/environment.py
    example-projects/multi-path-settings/.vscode/settings.json
    example-projects/multi-path-settings/features/settings_a.feature
    example-projects/multi-path-settings/features/steps/steps.py
    example-projects/multi-path-settings/features/environment.py
    example-projects/multi-path-settings/features-alt/settings_b.feature
    example-projects/multi-path-settings/features-alt/steps/steps.py
    example-projects/multi-path-settings/features-alt/environment.py
  </files>
  <read_first>
    example-projects/watcher-integration/behave.ini
    example-projects/watcher-integration/features/discovery.feature
    example-projects/watcher-integration/features/steps/steps.py
    example-projects/watcher-integration/features/environment.py
    example-projects/watcher-integration/features-alt/alt.feature
    example-projects/watcher-integration/features-alt/steps/steps.py
  </read_first>
  <action>
Create two fixture directories matching the established `watcher-integration/` patterns exactly.

**multi-path/ fixture (per D-01, D-03):**

1. `behave.ini` — initial single-path config:
   ```ini
   [behave]
   paths = features
   ```
   (Tests in Plan 02 will edit this to add `features-alt` and revert it back.)

2. `features/multi_path_a.feature` — primary path, **2 scenarios** (per D-03 — different counts make tree assertions unambiguous):
   ```gherkin
   Feature: Multi Path Primary

      Scenario: primary path test one
         Given we have behave installed
         When we implement a successful test
         Then we will see the result

      Scenario: primary path test two
         Given we have behave installed
         When we implement a successful test
         Then we will see the result
   ```

3. `features/steps/steps.py` — copy verbatim from `watcher-integration/features/steps/steps.py`.

4. `features/environment.py` — copy verbatim from `watcher-integration/features/environment.py`.

5. `features-alt/multi_path_b.feature` — dormant alt path, **1 scenario** (per D-03):
   ```gherkin
   Feature: Multi Path Alternate

      Scenario: alternate path test one
         Given we have behave installed
         When we implement a successful test
         Then we will see the result
   ```

6. `features-alt/steps/steps.py` — copy verbatim from `watcher-integration/features-alt/steps/steps.py` (same step implementations).

7. `features-alt/environment.py` — copy verbatim from `watcher-integration/features/environment.py`.

**multi-path-settings/ fixture (per D-06):**

Same directory layout as `multi-path/` but with NO `behave.ini`. Instead:

1. `.vscode/settings.json`:
   ```json
   {
     "gs-behave-bdd.featuresPaths": ["features", "features-alt"]
   }
   ```

2. `features/settings_a.feature` — **2 scenarios**:
   ```gherkin
   Feature: Settings Multi Path Primary

      Scenario: settings primary test one
         Given we have behave installed
         When we implement a successful test
         Then we will see the result

      Scenario: settings primary test two
         Given we have behave installed
         When we implement a successful test
         Then we will see the result
   ```

3. `features/steps/steps.py` — same steps.py content as multi-path.

4. `features/environment.py` — same environment.py content.

5. `features-alt/settings_b.feature` — **1 scenario**:
   ```gherkin
   Feature: Settings Multi Path Alternate

      Scenario: settings alternate test one
         Given we have behave installed
         When we implement a successful test
         Then we will see the result
   ```

6. `features-alt/steps/steps.py` — same steps.py content.

7. `features-alt/environment.py` — same environment.py content.
  </action>
  <verify>
    <automated>test -f example-projects/multi-path/behave.ini && test -f example-projects/multi-path/features/multi_path_a.feature && test -f example-projects/multi-path/features-alt/multi_path_b.feature && test -f example-projects/multi-path-settings/.vscode/settings.json && test -f example-projects/multi-path-settings/features/settings_a.feature && test -f example-projects/multi-path-settings/features-alt/settings_b.feature && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `example-projects/multi-path/behave.ini` contains exactly `paths = features` (not multi-path yet — tests edit it)
    - `example-projects/multi-path/features/multi_path_a.feature` contains exactly 2 `Scenario:` lines
    - `example-projects/multi-path/features-alt/multi_path_b.feature` contains exactly 1 `Scenario:` line
    - `example-projects/multi-path-settings/.vscode/settings.json` contains `"gs-behave-bdd.featuresPaths": ["features", "features-alt"]`
    - No `behave.ini` exists in `example-projects/multi-path-settings/`
    - `example-projects/multi-path-settings/features/settings_a.feature` contains exactly 2 `Scenario:` lines
    - `example-projects/multi-path-settings/features-alt/settings_b.feature` contains exactly 1 `Scenario:` line
    - All `steps.py` files contain `@given("we have behave installed")` decorator
    - All `environment.py` files contain `def before_scenario`
  </acceptance_criteria>
  <done>Both multi-path/ and multi-path-settings/ fixtures exist with correct structure, scenario counts differ per root (2 vs 1), and step/env files match watcher-integration patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Create monorepo-scan/ fixture + logSettings plural unit test</name>
  <files>
    example-projects/monorepo-scan/node_modules/.gitkeep
    example-projects/monorepo-scan/app-a/behave.ini
    example-projects/monorepo-scan/app-a/features/app_a.feature
    example-projects/monorepo-scan/app-a/features/steps/steps.py
    example-projects/monorepo-scan/app-a/features/environment.py
    example-projects/monorepo-scan/app-b/behave.ini
    example-projects/monorepo-scan/app-b/features/app_b.feature
    example-projects/monorepo-scan/app-b/features/steps/steps.py
    example-projects/monorepo-scan/app-b/features/environment.py
    example-projects/monorepo-scan/packages/app-c/behave.ini
    example-projects/monorepo-scan/packages/app-c/features/app_c.feature
    example-projects/monorepo-scan/packages/app-c/features/steps/steps.py
    example-projects/monorepo-scan/packages/app-c/features/environment.py
    test/unit/settings/logSettingsPlural.test.ts
  </files>
  <read_first>
    example-projects/watcher-integration/features/steps/steps.py
    example-projects/watcher-integration/features/environment.py
    example-projects/watcher-integration/behave.ini
    test/unit/settings/verboseLogging.test.ts
    src/settings.ts
    .gitignore
  </read_first>
  <action>
**monorepo-scan/ fixture (per D-02, D-04):**

Create three nested behave projects at different depths:

1. `node_modules/.gitkeep` — empty file. Because `.gitignore` contains `node_modules`, you MUST force-add this file: `git add -f example-projects/monorepo-scan/node_modules/.gitkeep`. (Per D-04: trust unit tests (TEST-11) for scanner perf; this just needs to exist so the BFS scanner has a directory to skip.)

2. `app-a/behave.ini` (depth 1):
   ```ini
   [behave]
   paths = features
   ```

3. `app-a/features/app_a.feature` — **2 scenarios** (per D-03):
   ```gherkin
   Feature: Monorepo App A

      Scenario: app a test one
         Given we have behave installed
         When we implement a successful test
         Then we will see the result

      Scenario: app a test two
         Given we have behave installed
         When we implement a successful test
         Then we will see the result
   ```

4. `app-a/features/steps/steps.py` — same steps.py as watcher-integration.

5. `app-a/features/environment.py` — same environment.py as watcher-integration.

6. `app-b/behave.ini` (depth 1):
   ```ini
   [behave]
   paths = features
   ```

7. `app-b/features/app_b.feature` — **1 scenario**:
   ```gherkin
   Feature: Monorepo App B

      Scenario: app b test one
         Given we have behave installed
         When we implement a successful test
         Then we will see the result
   ```

8. `app-b/features/steps/steps.py` — same steps.py.

9. `app-b/features/environment.py` — same environment.py.

10. `packages/app-c/behave.ini` (depth 2):
    ```ini
    [behave]
    paths = features
    ```

11. `packages/app-c/features/app_c.feature` — **1 scenario**:
    ```gherkin
    Feature: Monorepo App C

       Scenario: app c test one
          Given we have behave installed
          When we implement a successful test
          Then we will see the result
    ```

12. `packages/app-c/features/steps/steps.py` — same steps.py.

13. `packages/app-c/features/environment.py` — same environment.py.

**logSettings plural unit test (per D-09):**

Create `test/unit/settings/logSettingsPlural.test.ts`. Follow the exact pattern from `verboseLogging.test.ts`:

1. Import `WorkspaceSettings`, `WindowSettings`, `sinon`, `vscode`, and `commonModule`/`configModule` stubs.
2. Use `makeFakeWkspSettings()` helper but with MULTI-PATH values:
   ```typescript
   featuresUris: [
     vscode.Uri.file('/fake/workspace/features'),
     vscode.Uri.file('/fake/workspace/features-alt')
   ],
   projectRelativeFeaturesPaths: ['features', 'features-alt'],
   ```
3. Call `WorkspaceSettings.prototype.logSettings.call(fakeWksp, mockLogger, winSettings)`.
4. Parse the logged JSON and assert:
   - `parsed.fullFeaturesPaths` contains both `/fake/workspace/features` and `/fake/workspace/features-alt` separated by `, `
   - `parsed.featuresPaths` equals `"features, features-alt"`

5. Also add a single-path control test to prove single-path still works:
   ```typescript
   featuresUris: [vscode.Uri.file('/fake/workspace/features')],
   projectRelativeFeaturesPaths: ['features'],
   ```
   Assert `parsed.featuresPaths` equals `"features"` (no comma, no trailing space).
  </action>
  <verify>
    <automated>test -f example-projects/monorepo-scan/app-a/behave.ini && test -f example-projects/monorepo-scan/app-b/behave.ini && test -f example-projects/monorepo-scan/packages/app-c/behave.ini && test -f example-projects/monorepo-scan/node_modules/.gitkeep && npm run test:unit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `example-projects/monorepo-scan/app-a/behave.ini` contains `paths = features`
    - `example-projects/monorepo-scan/app-b/behave.ini` contains `paths = features`
    - `example-projects/monorepo-scan/packages/app-c/behave.ini` contains `paths = features`
    - `example-projects/monorepo-scan/node_modules/.gitkeep` exists (empty file)
    - `app-a/features/app_a.feature` has exactly 2 `Scenario:` lines
    - `app-b/features/app_b.feature` has exactly 1 `Scenario:` line
    - `packages/app-c/features/app_c.feature` has exactly 1 `Scenario:` line
    - `test/unit/settings/logSettingsPlural.test.ts` exists
    - `npm run test:unit` passes — the new test asserts `parsed.fullFeaturesPaths` contains both paths comma-separated
    - `npm run test:unit` output includes `logSettings plural output` suite name
    - `npx eslint src --ext ts` exits 0
  </acceptance_criteria>
  <done>monorepo-scan/ fixture exists with 3 nested configs at depths 1 and 2 plus node_modules/ stub; logSettings unit test proves plural paths are comma-joined; npm run test:unit passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan creates test fixtures (static files) and a unit test.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T (Tampering) | Fixture files | accept | Test fixtures are static, repo-controlled, no user input processed |
</threat_model>

<verification>
1. All three fixture directories exist with correct file counts:
   - `multi-path/`: 7 files (1 ini + 2 features + 2 steps.py + 2 environment.py)
   - `multi-path-settings/`: 7 files (1 settings.json + 2 features + 2 steps.py + 2 environment.py)
   - `monorepo-scan/`: 13 files (1 .gitkeep + 3 ini + 3 features + 3 steps.py + 3 environment.py)
2. Scenario counts differ per root in every fixture (2 vs 1) for unambiguous tree assertions.
3. `npm run test:unit` passes with new logSettings plural test.
4. `npx eslint src --ext ts` exits 0.
</verification>

<success_criteria>
- Three isolated fixture directories match D-01, D-02, D-03, D-04, D-06 exactly.
- logSettings plural output verified by unit test (D-09/SC-5).
- No existing tests broken.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ux-polish-regression-hardening/11-01-SUMMARY.md`
</output>
