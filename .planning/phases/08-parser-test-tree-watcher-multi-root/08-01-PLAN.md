---
phase: 08-parser-test-tree-watcher-multi-root
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/parsers/configParser.ts
  - src/handlers/configDiagnostics.ts
  - src/common.ts
  - test/unit/parsers/configParser.test.ts
autonomous: true
requirements: [MP-01, MP-04, TEST-10]

must_haves:
  truths:
    - "Overlapping paths (parent contains child) are deduped — only the broader parent survives"
    - "Exact duplicate paths (same URI, case-insensitive) are deduped to one entry"
    - "When some paths in paths= fail to resolve, valid paths still work (partial success)"
    - "Each invalid path gets an Error diagnostic in the Problems panel attached to the config file at the correct line"
    - "Subsumption collisions produce a Warning diagnostic on the config file + info log in the output channel"
    - "When ALL paths fail to resolve, config is treated as malformed (no convention fallback)"
  artifacts:
    - path: "src/parsers/configParser.ts"
      provides: "Line number tracking per path entry in BehaveConfigResult"
      contains: "pathLineNumbers"
    - path: "src/common.ts"
      provides: "dedupResolvedPaths function + hasFeaturesFolder Branch B multi-path with partial success"
      contains: "dedupResolvedPaths"
    - path: "src/handlers/configDiagnostics.ts"
      provides: "Per-path resolution failure + subsumption diagnostic functions"
      contains: "setPathResolutionDiagnostics"
    - path: "test/unit/parsers/configParser.test.ts"
      provides: "Tests for dedup, subsumption, line tracking, partial success"
  key_links:
    - from: "src/parsers/configParser.ts"
      to: "src/common.ts"
      via: "resolvedPaths[] + pathLineNumbers[] consumed by hasFeaturesFolder"
      pattern: "configResult\\.resolvedPaths"
    - from: "src/common.ts"
      to: "src/handlers/configDiagnostics.ts"
      via: "diagnostic functions called on invalid/subsumed paths"
      pattern: "setPathResolutionDiagnostics|setSubsumptionDiagnostics"
---

<objective>
Add path dedup/subsumption logic, line number tracking for diagnostic attachment, and per-path resolution failure diagnostics. When a behave config lists multiple paths and some don't exist, valid paths proceed normally while each invalid path gets an Error diagnostic in the Problems panel attached to the config file at the exact line. Overlapping paths (parent contains child) are deduped with a Warning diagnostic.

Purpose: Enables multi-path partial success (MP-04) and prevents duplicate feature discovery from overlapping paths (MP-01). Foundation for Plans 02/03 which iterate the deduped paths.
Output: Enriched config parser, dedup function, diagnostic infrastructure, and comprehensive unit tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-parser-test-tree-watcher-multi-root/08-CONTEXT.md

@src/parsers/configParser.ts
@src/common.ts
@src/handlers/configDiagnostics.ts
@test/unit/parsers/configParser.test.ts
@AI_INSTRUCTIONS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/parsers/configParser.ts (current):
```typescript
export type BehaveConfigResult =
  | { ok: true; configFileUri: vscode.Uri; format: 'ini' | 'toml'; rawPaths: string[]; resolvedPaths: vscode.Uri[] }
  | { ok: false; configFileUri: vscode.Uri; errorMessage: string };

export function findBehaveConfig(wkspUri: vscode.Uri): BehaveConfigResult | undefined;
```

From src/common.ts (current):
```typescript
export interface DiscoveryEntry {
  source: DiscoverySource;
  configFileUri?: vscode.Uri;
  configError?: { configFileUri: vscode.Uri; errorMessage: string };
  featuresUris: vscode.Uri[];
}

export const uriId = (uri: vscode.Uri): string => uri.toString().toLowerCase();
```

From src/handlers/configDiagnostics.ts (current):
```typescript
export function setConfigParseErrorDiagnostic(configFileUri: vscode.Uri, errorMessage: string): void;
export function clearConfigParseErrorDiagnostic(configFileUri: vscode.Uri): void;
// Uses config.diagnostics (shared DiagnosticCollection from configuration.ts)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: configParser line tracking + dedup function + diagnostic infra</name>
  <files>src/parsers/configParser.ts, src/common.ts, src/handlers/configDiagnostics.ts</files>
  <read_first>
    - src/parsers/configParser.ts (full file — understand INI/TOML parsers, resolvePaths, buildResult, BehaveConfigResult type)
    - src/common.ts (lines 35-45 for DiscoveryEntry, lines 177-300 for hasFeaturesFolder, lines 340-370 for getFeaturesRootForFile and uriId)
    - src/handlers/configDiagnostics.ts (full file — existing diagnostic pattern with config.diagnostics collection)
    - .planning/phases/08-parser-test-tree-watcher-multi-root/08-CONTEXT.md (D-04 through D-11)
  </read_first>
  <action>
**A. Enrich BehaveConfigResult with line numbers (configParser.ts):**

1. Add `pathLineNumbers: number[]` to the `ok: true` variant of `BehaveConfigResult`:
   ```typescript
   | { ok: true; configFileUri: vscode.Uri; format: 'ini' | 'toml'; rawPaths: string[]; resolvedPaths: vscode.Uri[]; pathLineNumbers: number[] }
   ```
   `pathLineNumbers[i]` is the 0-indexed line number in the config file where `rawPaths[i]` appears.

2. **INI parser (`parseIniConfig`):** Add a `let lineIndex = 0` counter before the `for (const line of lines)` loop. Increment `lineIndex` at the end of each iteration. When collecting paths:
   - For the `paths =` key line: if value is non-empty, push `lineIndex` to a new `pathLineNumbers: number[]` array alongside `pathsLines.push(value)`.
   - For continuation lines: push `lineIndex` when `pathsLines.push(trimmed)`.
   - Pass `pathLineNumbers` through to `buildResult`.

3. **TOML parser (`parseTomlConfig`):** After parsing with smol-toml, derive line numbers from the raw text. Split `content` into lines. For each `rawPaths[i]`, search the lines array starting from the `[tool.behave]` section for a line containing the path string value. Store the 0-indexed line number. If not found, default to 0 (the `[tool.behave]` header line).

4. **Update `buildResult`:** Accept `pathLineNumbers: number[]` as a parameter, include in the returned object.

**B. Add dedup function (common.ts):**

Add an exported `dedupResolvedPaths` function after the `getFeaturesRootForFile` function (around line 370):

```typescript
export interface SubsumedPath {
  rawPath: string;
  lineNumber: number;
  subsumedBy: string;
}

export interface DedupResult {
  resolvedPaths: vscode.Uri[];
  rawPaths: string[];
  pathLineNumbers: number[];
  subsumedPaths: SubsumedPath[];
}

export function dedupResolvedPaths(
  resolvedPaths: vscode.Uri[],
  rawPaths: string[],
  pathLineNumbers: number[]
): DedupResult {
  // ... (see algorithm below)
}
```

Algorithm per D-09, D-11:
1. Create indexed entries: `{ uri, rawPath, lineNumber, id: uriId(uri) }` for each input.
2. Sort by `uri.path.length` ascending (broader paths first — parent wins per D-09).
3. Iterate: for each entry, check if `seenIds` already has `entry.id` (exact duplicate) or if any accepted entry's `uri.path + '/'` is a prefix of `entry.uri.path` (subsumption). If either, record as `SubsumedPath` and skip. Otherwise accept and add `entry.id` to `seenIds`.
4. Return accepted paths + subsumed list.

**C. Update hasFeaturesFolder Branch B (common.ts):**

Replace the existing Branch B block (lines ~260-280) with multi-path logic:

1. After `findBehaveConfig`, if `configResult.ok`:
   - Call `dedupResolvedPaths(configResult.resolvedPaths, configResult.rawPaths, configResult.pathLineNumbers)`.
   - Check `fs.existsSync` per deduped path. Partition into `validPaths: Uri[]` and `invalidPaths: { uri, lineNumber, rawPath }[]`.
   - If `invalidPaths.length > 0`: call `setPathResolutionDiagnostics(configResult.configFileUri, invalidPaths)`.
   - If `dedupResult.subsumedPaths.length > 0`: call `setSubsumptionDiagnostics(configResult.configFileUri, dedupResult.subsumedPaths)` and log each subsumption via `config.logger.logInfo(...)`.
   - If `validPaths.length > 0`: set `discoveryCache` with `featuresUris: validPaths` (D-04 partial success).
   - If `validPaths.length === 0`: ALL paths failed → emit per-path Error diagnostics (already done above) and **do NOT fall through to convention** per D-06. Return false.
2. Clear any stale path diagnostics for this config file at the start of Branch B: call `clearPathDiagnostics(configResult.configFileUri)` before emitting new ones.

**D. Add diagnostic functions (configDiagnostics.ts):**

Add three new functions following the existing pattern (use `config.diagnostics` collection, filter by diagnostic code):

1. `setPathResolutionDiagnostics(configFileUri, invalidPaths: { rawPath: string; lineNumber: number }[])`:
   - Diagnostic code: `'behave-config-path-not-found'`
   - Severity: `Error` (per D-04)
   - Message: `Behave config: path "${rawPath}" does not exist`
   - Range: `new vscode.Range(lineNumber, 0, lineNumber, 999)` (highlight the entire line)

2. `setSubsumptionDiagnostics(configFileUri, subsumedPaths: SubsumedPath[])`:
   - Diagnostic code: `'behave-config-path-subsumed'`
   - Severity: `Warning` (per D-10)
   - Message: `Behave config: path "${rawPath}" is contained within "${subsumedBy}" — this path will be ignored to avoid duplicate features`
   - Range: `new vscode.Range(lineNumber, 0, lineNumber, 999)`

3. `clearPathDiagnostics(configFileUri)`:
   - Remove all diagnostics with code `'behave-config-path-not-found'` or `'behave-config-path-subsumed'` for the given file.

All three functions must preserve existing diagnostics with different codes (filter + append pattern matching `setConfigParseErrorDiagnostic`).
  </action>
  <acceptance_criteria>
    - configParser.ts BehaveConfigResult ok:true type contains `pathLineNumbers: number[]`
    - configParser.ts parseIniConfig builds pathLineNumbers parallel to rawPaths using a line counter
    - configParser.ts parseTomlConfig derives pathLineNumbers from raw text search
    - configParser.ts buildResult signature includes pathLineNumbers parameter
    - common.ts exports `dedupResolvedPaths` function
    - common.ts exports `SubsumedPath` interface with `rawPath`, `lineNumber`, `subsumedBy` fields
    - common.ts exports `DedupResult` interface
    - common.ts hasFeaturesFolder Branch B calls `dedupResolvedPaths` before `fs.existsSync` checks
    - common.ts hasFeaturesFolder Branch B sets `featuresUris` to array of valid paths (not just `[resolvedPaths[0]]`)
    - common.ts hasFeaturesFolder Branch B returns false when ALL deduped paths fail existsSync (D-06)
    - configDiagnostics.ts exports `setPathResolutionDiagnostics` function
    - configDiagnostics.ts exports `setSubsumptionDiagnostics` function
    - configDiagnostics.ts exports `clearPathDiagnostics` function
    - `npx eslint src --ext ts` exits 0
    - `npm run test:unit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx eslint src --ext ts &amp;&amp; npm run test:unit</automated>
  </verify>
  <done>BehaveConfigResult carries line numbers, dedupResolvedPaths deduplicates overlapping/duplicate paths, hasFeaturesFolder Branch B uses multi-path partial success, diagnostic functions emit per-path Error/Warning diagnostics. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for dedup, subsumption, line tracking, and partial success</name>
  <files>test/unit/parsers/configParser.test.ts</files>
  <read_first>
    - test/unit/parsers/configParser.test.ts (full file — understand existing test patterns, fixture structure, mock setup)
    - src/parsers/configParser.ts (current state after Task 1)
    - src/common.ts (dedupResolvedPaths signature after Task 1)
    - test/unit/parsers/fixtures/ (existing fixture directory structure)
  </read_first>
  <action>
Add new test suites to `test/unit/parsers/configParser.test.ts` covering TEST-10 requirements:

**A. Line number tracking tests (new `describe("pathLineNumbers")` block):**

1. **INI single-line paths:** Parse a behave.ini with `paths = features` — assert `pathLineNumbers[0]` equals the 0-indexed line number of the `paths =` line.
2. **INI continuation lines:** Parse a behave.ini with:
   ```
   [behave]
   paths = features
     features-alt
     features-api
   ```
   Assert `pathLineNumbers` is `[lineOfPathsKey, lineOfFeaturesAlt, lineOfFeaturesApi]` (each continuation line's 0-indexed number).
3. **INI with comments/blanks between sections:** Ensure line numbers are correct even with comment lines and blank lines before `[behave]`.
4. **TOML paths array:** Parse a pyproject.toml with `paths = ["features", "features-alt"]` — assert line numbers point to lines containing each string value.

Create minimal fixture files in `test/unit/parsers/fixtures/config/` for these tests (e.g., `multi-path-lines/behave.ini`, `multi-path-toml/pyproject.toml`).

**B. Dedup behavior tests (new `describe("dedupResolvedPaths")` block):**

Import `dedupResolvedPaths` from `../../src/common` (adjust path for test compilation).

5. **Exact duplicate:** Two identical paths → deduplicated to one, subsumedPaths contains the duplicate.
6. **Case-insensitive duplicate (D-11):** `features` vs `Features` on Windows-style URIs → deduped via `uriId()`.
7. **Subsumption — parent wins (D-09):** `features` + `features/api` → only `features` survives, `features/api` in subsumedPaths with `subsumedBy: "features"`.
8. **Reverse order subsumption:** `features/api` + `features` (child listed first) → still deduped correctly (parent wins regardless of order).
9. **No overlap:** `features` + `features-alt` → both survive, no subsumedPaths.
10. **Three paths with mixed overlap:** `features`, `features/api`, `features-alt` → `features` + `features-alt` survive.

**C. Partial success tests (new `describe("multi-path partial success")`):**

These test the hasFeaturesFolder integration. Use the existing test harness pattern (mock fs.existsSync or use real fixture directories).

11. **Some paths exist, some don't:** Config with `paths = features\n  bogus` where `features/` exists but `bogus/` doesn't → discovery succeeds with `featuresUris.length === 1` containing only `features/`.
12. **All paths fail (D-06):** Config with `paths = bogus1\n  bogus2` where neither exists → discovery returns false, no convention fallback.
13. **Empty paths after dedup:** Config with `paths = features\n  features/api` (subsumption removes `features/api`) + `features/` exists → `featuresUris.length === 1`.

For tests 11-13, create fixture directories under `test/unit/parsers/fixtures/config/` with appropriate behave.ini files and directory structures.
  </action>
  <acceptance_criteria>
    - configParser.test.ts contains `describe("pathLineNumbers")` with at least 4 test cases
    - configParser.test.ts contains `describe("dedupResolvedPaths")` with at least 6 test cases
    - configParser.test.ts contains `describe("multi-path partial success")` with at least 3 test cases
    - All new tests pass: `npm run test:unit` exits 0
    - Line number tests verify exact 0-indexed line numbers for INI continuation lines and TOML array entries
    - Dedup tests verify SubsumedPath.subsumedBy contains the winning path's rawPath string
    - Partial success test verifies featuresUris only contains existing paths
    - `npx eslint src --ext ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx eslint src --ext ts &amp;&amp; npm run test:unit</automated>
  </verify>
  <done>13+ new unit tests covering line tracking (INI + TOML), dedup (exact/case/subsumption), and partial success. All 580+ tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config file → extension | Config files are workspace-local, read-only. Already parsed in v1.0. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | D (DoS) | dedupResolvedPaths | accept | O(n²) subsumption check where n = paths count in config. n is practically < 20; no risk. |
| T-08-02 | I (Info Disclosure) | diagnostic messages | accept | Diagnostic messages show path names from the user's own config file — not sensitive. |
</threat_model>

<verification>
- `npx eslint src --ext ts` exits 0
- `npm run test:unit` passes (all existing + 13+ new tests)
- INI with `paths = features\n  features-alt` produces `pathLineNumbers` matching actual line indices
- `dedupResolvedPaths(["features", "features/api"])` returns only `features`, subsumedPaths contains `features/api`
- hasFeaturesFolder with mixed valid/invalid paths sets `featuresUris` to valid-only subset
</verification>

<success_criteria>
Config parser carries line numbers per path entry. Overlapping/duplicate paths are deduped before discovery. Invalid paths produce Error diagnostics. Subsumption produces Warning diagnostics. Partial success works (valid paths proceed, invalid paths flagged). All existing tests pass, 13+ new tests added.
</success_criteria>

<output>
After completion, create `.planning/phases/08-parser-test-tree-watcher-multi-root/08-01-SUMMARY.md`
</output>
