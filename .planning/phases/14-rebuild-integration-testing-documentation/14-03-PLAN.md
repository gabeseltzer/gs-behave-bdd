---
phase: 14-rebuild-integration-testing-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "README lists auto-discovery and project switching in the 'New in this fork' feature list"
    - "README has a dedicated 'Auto-Discovery & Project Switching' section with brief descriptions"
    - "README frames projectPath and featuresPath as manual overrides of auto-discovery"
    - "README documentation uses text only — no directory tree diagrams, no config examples, no screenshots"
  artifacts:
    - path: "README.md"
      provides: "Auto-discovery, multi-path, monorepo scanning, and project switching documentation"
      contains: "Auto-Discovery & Project Switching"
  key_links:
    - from: "README.md '## Auto-Discovery & Project Switching'"
      to: "README.md '### New in this fork'"
      via: "bullet point reference"
      pattern: "auto-discover|project switching"
---

<objective>
Document the complete auto-discovery feature set in README.md.

Purpose: Closes DOC-01. Users need to know the extension auto-discovers behave projects, supports multi-path configs, scans monorepo subdirectories, and enables project switching — all without needing to manually configure settings.

Output: Updated README.md with feature list additions and dedicated section.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-rebuild-integration-testing-documentation/14-CONTEXT.md

### Current README
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auto-discovery bullet to "New in this fork" and dedicated section</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — understand current structure, section ordering, existing feature list, projectPath/featuresPath documentation)
  </read_first>
  <action>
**Step 1: Add bullet points to "New in this fork" list (D-05)**

After the existing item 9 ("Fix issues with step parsing..."), add:

```markdown
10. **Auto-discovery** of behave projects from native config files (`behave.ini`, `.behaverc`, `setup.cfg`, `tox.ini`, `pyproject.toml`). No `featuresPath` or `projectPath` settings needed in most cases.
11. **Monorepo support** with automatic subdirectory scanning for behave configs. Multi-path `paths` entries in config files are fully supported.
12. **Project switching** for workspaces with multiple behave projects. A status bar indicator and "Select Project" command let you switch between discovered projects.
```

**Step 2: Add dedicated section (D-05)**

Add a new section `## Auto-Discovery & Project Switching` BEFORE the existing `## Extension settings` section (after the `---` that follows "Required project directory structure"). Content per D-06 (brief text only) and D-07 (no diagrams/screenshots):

```markdown
## Auto-Discovery & Project Switching

The extension automatically discovers your behave project by reading native behave configuration files (`behave.ini`, `.behaverc`, `setup.cfg`, `tox.ini`, `pyproject.toml`). If any of these files exist in your workspace, the extension reads the `paths` setting and configures itself — no manual `featuresPath` or `projectPath` settings needed.

### Multi-path configs

If your behave config specifies multiple paths (e.g. `paths = tests/features other_tests/features`), all paths are used for test discovery and step navigation.

### Monorepo scanning

For monorepos, the extension scans subdirectories (up to a configurable depth) looking for behave config files. The first discovered project becomes active automatically. You can control scan depth with the `discoveryDepth` setting (default: 3) and stop on the first hit with `discoveryStopOnFirstHit`.

### Project switching

When multiple behave projects are discovered in a workspace, a status bar indicator shows which project is active. Click it or use the **"Behave BDD: Select Project"** command to switch between projects. Switching triggers a full rebuild of the test tree and step navigation.

The status bar indicator is hidden when only one project exists or when `projectPath` is manually set.
```

**Step 3: Update projectPath/featuresPath documentation to frame as manual overrides (D-08)**

Find the paragraph that starts with "If your features folder is not called..." (around line 95) and the "If your behave project is not in the workspace root..." paragraph (around line 107). Add a note before these paragraphs:

```markdown
> **Note:** In most cases you don't need these settings. The extension auto-discovers your behave project from config files. Use `projectPath` and `featuresPath` only as manual overrides when auto-discovery doesn't suit your setup.
```

Place this note just before the existing "If your features folder is not called..." paragraph. Do NOT delete the existing documentation — the examples are still useful for users who need manual overrides.
  </action>
  <verify>
    <automated>Select-String -Path README.md -Pattern "Auto-Discovery" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Auto-Discovery & Project Switching` as a section header
    - README.md "New in this fork" list contains an item mentioning "Auto-discovery"
    - README.md "New in this fork" list contains an item mentioning "Monorepo support"
    - README.md "New in this fork" list contains an item mentioning "Project switching"
    - README.md contains "In most cases you don't need these settings" note near projectPath/featuresPath docs
    - README.md contains "Select Project" in the Auto-Discovery section
    - README.md does NOT contain new directory tree diagrams (```text blocks) in the new sections
    - README.md does NOT contain new config file examples (```ini or ```json blocks) in the new sections
    - Existing projectPath/featuresPath examples are preserved (not deleted)
  </acceptance_criteria>
  <done>README documents auto-discovery, multi-path, monorepo scanning, and project switching with brief text descriptions; existing docs preserved and framed as manual overrides</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only change; no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No security-relevant changes in this plan |
</threat_model>

<verification>
1. `Select-String -Path README.md -Pattern "Auto-Discovery & Project Switching"` returns a match
2. `Select-String -Path README.md -Pattern "In most cases you don't need these settings"` returns a match
3. Manual review: no new ```text, ```ini, ```json blocks in the new sections
</verification>

<success_criteria>
- README has auto-discovery bullet points in "New in this fork"
- README has dedicated "Auto-Discovery & Project Switching" section with sub-sections for multi-path, monorepo, and switching
- Existing projectPath/featuresPath documentation is preserved but framed as manual overrides
- No config file examples, directory trees, or screenshots in the new sections (D-06, D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/14-rebuild-integration-testing-documentation/14-03-SUMMARY.md`
</output>
