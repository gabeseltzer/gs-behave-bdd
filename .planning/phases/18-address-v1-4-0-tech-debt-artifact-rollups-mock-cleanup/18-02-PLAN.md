---
phase: 18-address-v1-4-0-tech-debt-artifact-rollups-mock-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/16-deprecate-featurespath/16-SUMMARY.md
  - .planning/phases/17-cross-cutting-verification/17-SUMMARY.md
  - .planning/phases/17-cross-cutting-verification/17-VERIFICATION.md
  - AI_INSTRUCTIONS.md
  - .planning/STATE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 16 has a phase-level SUMMARY.md aggregating plans 16-01..16-06 and listing all 7 DEP requirements as completed"
    - "Phase 17 has BOTH a phase-level SUMMARY.md and a phase-level VERIFICATION.md (status: passed) aggregating plans 17-01..17-03"
    - "AI_INSTRUCTIONS.md documents the multiroot integration mutex flake (`Another instance of app 'Code' is already active`) as an environmental — not a regression — issue with the suggested workaround"
    - "STATE.md learnings/follow-up section carries an entry for the v1.4.0 cache-invalidation tech debt so it is not forgotten"
  artifacts:
    - path: ".planning/phases/16-deprecate-featurespath/16-SUMMARY.md"
      provides: "Phase 16 rollup with frontmatter `requirements-completed: [DEP-01..DEP-07]` and `status: verified`"
      contains: "all seven DEP-* IDs"
      min_lines: 50
    - path: ".planning/phases/17-cross-cutting-verification/17-SUMMARY.md"
      provides: "Phase 17 rollup aggregating 17-01..17-03 plus VALIDATION lessons"
      contains: "migrations integration suite"
      min_lines: 50
    - path: ".planning/phases/17-cross-cutting-verification/17-VERIFICATION.md"
      provides: "Phase 17 verification report with `status: passed`"
      contains: "status: passed"
    - path: "AI_INSTRUCTIONS.md"
      provides: "contributor note on integration-suite mutex flake"
      contains: "Another instance of app"
    - path: ".planning/STATE.md"
      provides: "carry-forward note for activeProjectCache invalidation follow-up"
      contains: "activeProjectCache"
  key_links:
    - from: ".planning/phases/16-deprecate-featurespath/16-SUMMARY.md"
      to: "16-VERIFICATION.md + per-plan SUMMARYs"
      via: "aggregated facts (no new analysis)"
      pattern: "DEP-0"
    - from: ".planning/phases/17-cross-cutting-verification/17-SUMMARY.md"
      to: "commits 27e5af3 + c08ced5 + 15-HUMAN-UAT closeout"
      via: "evidence citations in the rollup"
      pattern: "27e5af3|c08ced5"
    - from: "AI_INSTRUCTIONS.md Integration Test Structure section"
      to: "developer running `npm run test:integration` locally"
      via: "one-paragraph env-flake callout"
      pattern: "Another instance of app"
---

<objective>
Close the artifact-rollup gaps from `.planning/v1.4.0-MILESTONE-AUDIT.md` plus the two doc-only items the audit flags as low-severity. No code changes.

Specifically:
1. **Phase 16 phase-level SUMMARY** — does not exist; per-plan summaries (16-01..16-06) and `16-VERIFICATION.md` already cover all the facts. Aggregate.
2. **Phase 17 phase-level SUMMARY + VERIFICATION** — neither exists; per-plan summaries (17-01..17-03) and `17-VALIDATION.md` cover the facts. Aggregate. VERIFICATION marks Phase 17 `passed` with explicit citations to commits 27e5af3 (suite registration), c08ced5 (cache fix), and the Phase 15 HUMAN-UAT closeout.
3. **Multiroot mutex flake** — add a paragraph to `AI_INSTRUCTIONS.md` § "Integration Test Structure" warning local devs that running their own VS Code at the same time as `npm run test:integration` causes `Another instance of app 'Code' is already active`. Recommend closing VS Code or using a separate user-data-dir.
4. **Cache-invalidation follow-up backlog entry** — add a one-paragraph entry to `.planning/STATE.md` (the project's existing learnings/state file) so the v1.4.0 audit's recommendation ("pair `clearScanResultCache()` with project-list invalidation when discovery-influencing settings change") survives milestone closure.

Purpose: Get Phase 18 out of the way as a clean closure phase so `/gsd-complete-milestone v1.4.0` can run with zero outstanding artifact debt. The `package.json` 1.3.0→1.4.0 bump is **explicitly out of scope** per the audit (see Out of Scope below).

Output: Five files written; no code changes; no test impact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.4.0-MILESTONE-AUDIT.md
@.planning/phases/15-notification-suppression/15-SUMMARY.md
@.planning/phases/16-deprecate-featurespath/16-VERIFICATION.md
@.planning/phases/16-deprecate-featurespath/16-01-SUMMARY.md
@.planning/phases/16-deprecate-featurespath/16-02-SUMMARY.md
@.planning/phases/16-deprecate-featurespath/16-03-SUMMARY.md
@.planning/phases/16-deprecate-featurespath/16-04-SUMMARY.md
@.planning/phases/16-deprecate-featurespath/16-05-SUMMARY.md
@.planning/phases/16-deprecate-featurespath/16-06-SUMMARY.md
@.planning/phases/17-cross-cutting-verification/17-01-SUMMARY.md
@.planning/phases/17-cross-cutting-verification/17-02-SUMMARY.md
@.planning/phases/17-cross-cutting-verification/17-03-SUMMARY.md
@.planning/phases/17-cross-cutting-verification/17-VALIDATION.md
@AI_INSTRUCTIONS.md
@.planning/STATE.md

<interfaces>
<!-- Reference template for Phase 16 SUMMARY frontmatter — mirror Phase 15. -->

From `.planning/phases/15-notification-suppression/15-SUMMARY.md` lines 1-15 (frontmatter pattern to mirror):
```yaml
---
phase: 16-deprecate-featurespath
milestone: v1.4.0
status: verified
verified_at: "2026-04-29T00:00:00Z"
plans_completed: 6
total_commits: <executor reads from per-plan summaries>
unit_tests_passing: 696
unit_tests_baseline: 683  # post-Phase-15
unit_tests_added: <delta>
requirements: [DEP-01, DEP-02, DEP-03, DEP-04, DEP-05, DEP-06, DEP-07]
decisions: [<list from 16-CONTEXT.md if executor wants; optional>]
created: 2026-04-27
completed: 2026-04-29
---
```

For Phase 17 SUMMARY: same shape but `requirements: []` (Phase 17 verifies, doesn't introduce new REQs — per audit "Phase 17 IS the cross-cutting integration verification phase").

For Phase 17 VERIFICATION: mirror `16-VERIFICATION.md` shape — frontmatter then "Goal Achievement", "Observable Truths" table, "Required Artifacts" table, "Requirements Coverage" (cross-link to NOTIF-* and DEP-* satisfied through Phase 17's integration suite), "Anti-Patterns Found: None.", "Gaps Summary: No gaps.".

Key evidence to cite in 17-VERIFICATION:
- `test/integration/migrations suite/extension.test.ts` — 7 tests, real VSCode (commit landed in 17-02)
- `test/integration/runTestSuites.ts` registration — commit 27e5af3 (per 17-03-SUMMARY)
- `src/common.ts` cache fix — commit c08ced5 (per 17-03-SUMMARY)
- `.planning/phases/15-notification-suppression/15-HUMAN-UAT.md` — `status: complete` (per 17-03-SUMMARY)
</interfaces>

<location_hints>
- `AI_INSTRUCTIONS.md` § "Integration Test Structure" starts at line 279. Add the mutex-flake paragraph there (or as a new sub-section like "Local-dev gotcha"). Keep it short — 4-6 lines.
- `.planning/STATE.md` — append a new section near existing learnings (or at the end of file). Heading like `## v1.4.0 Carry-Forward` is fine.
</location_hints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Phase 16 phase-level SUMMARY</name>
  <files>.planning/phases/16-deprecate-featurespath/16-SUMMARY.md</files>
  <action>
Create `.planning/phases/16-deprecate-featurespath/16-SUMMARY.md` aggregating per-plan summaries 16-01..16-06 plus the facts already captured in `16-VERIFICATION.md`. Mirror the structure of `15-SUMMARY.md` — same section ordering: a one-paragraph "What Shipped" lede, then `## Plan-by-Plan Recap`, `## Verification Results`, `## Requirement Traceability` (DEP-01..DEP-07), `## Decisions Honored` (optional — pull from 16-CONTEXT if available), `## Files Changed`, `## Phase Metrics`.

Frontmatter (mirror Phase 15 shape, see `<interfaces>` block):
- `phase: 16-deprecate-featurespath`
- `milestone: v1.4.0`
- `status: verified`
- `verified_at: "2026-04-29T00:00:00Z"`
- `plans_completed: 6`
- `requirements: [DEP-01, DEP-02, DEP-03, DEP-04, DEP-05, DEP-06, DEP-07]`
- `unit_tests_passing: 696`
- pull other counts (commits, tests added) from per-plan SUMMARYs

**This is a rollup, not new analysis.** Pull facts directly from the per-plan summaries and `16-VERIFICATION.md`. Do NOT re-verify; do NOT add new claims. If a fact isn't in the source artifacts, omit it.

Length target: ~150-250 lines (matches Phase 15 SUMMARY scale, scoped to actual content available).
  </action>
  <verify>
    <automated>test -f .planning/phases/16-deprecate-featurespath/16-SUMMARY.md &amp;&amp; grep -c "DEP-0" .planning/phases/16-deprecate-featurespath/16-SUMMARY.md | awk '$1 &gt;= 7 {exit 0} {exit 1}' &amp;&amp; grep -q "^status: verified" .planning/phases/16-deprecate-featurespath/16-SUMMARY.md</automated>
  </verify>
  <done>
- File exists at the expected path
- Frontmatter contains `phase: 16-deprecate-featurespath`, `status: verified`, `requirements: [DEP-01..DEP-07]`
- All 7 DEP-* IDs appear in the body (grep count ≥ 7)
- File length ≥ 50 lines (sanity check, not a target)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 17 phase-level SUMMARY and VERIFICATION</name>
  <files>.planning/phases/17-cross-cutting-verification/17-SUMMARY.md, .planning/phases/17-cross-cutting-verification/17-VERIFICATION.md</files>
  <action>
Create both Phase 17 phase-level rollup files in this single task — they share source material (per-plan summaries 17-01..17-03, plus 17-VALIDATION.md).

**File 1: `17-SUMMARY.md`** — mirror `15-SUMMARY.md` structure. Frontmatter:
- `phase: 17-cross-cutting-verification`
- `milestone: v1.4.0`
- `status: verified`
- `verified_at: "2026-04-30T17:35:00Z"` (from 17-03-SUMMARY)
- `plans_completed: 3`
- `requirements: []` (Phase 17 verifies, no new REQs)
- `requirements-verified: [DEP-01..DEP-07, NOTIF-01..NOTIF-08]` (the 15 it integration-tests)
- `unit_tests_passing: 696`

Body sections: lede paragraph noting the migrations integration suite is the single deliverable; `## Plan-by-Plan Recap` (17-01 fixture, 17-02 suite, 17-03 registration + regression fix); `## Verification Results` (table citing commits 27e5af3 and c08ced5, plus 15-HUMAN-UAT closeout); `## Notable Findings` (the Phase 12 cache-invalidation regression — describe the bisect, the read-time fix, and explicitly tag it as carry-forward tech debt with pointer to STATE.md entry from Task 4); `## Files Changed`; `## Phase Metrics`.

**File 2: `17-VERIFICATION.md`** — mirror `16-VERIFICATION.md` structure exactly. Frontmatter:
- `phase: 17-cross-cutting-verification`
- `verified: 2026-04-30T17:35:00Z`
- `status: passed`
- `score: <N>/<N> must-haves verified` (count from 17-* plan must_haves)

Sections: `# Phase 17: Cross-Cutting Verification — Verification Report`, `## Goal Achievement`, then tables for `### Observable Truths`, `### Required Artifacts`, `### Key Link Verification`, `### Requirements Coverage` (cross-link DEP-* and NOTIF-* to the 7 migrations-suite tests + commit citations), `### Anti-Patterns Found: None.`, `### Human Verification Required: None — automated migrations suite closed Phase 15's two pending HUMAN-UAT items (see 15-HUMAN-UAT.md status: complete).`, `### Gaps Summary: No gaps.`

Both are rollups — pull facts from `17-01-SUMMARY.md`, `17-02-SUMMARY.md`, `17-03-SUMMARY.md`, and `17-VALIDATION.md`. Do NOT re-run verification commands; do NOT introduce new claims. Cite commits exactly as the per-plan summaries cite them.

Length target: SUMMARY ~150-250 lines, VERIFICATION ~80-150 lines.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-cross-cutting-verification/17-SUMMARY.md &amp;&amp; test -f .planning/phases/17-cross-cutting-verification/17-VERIFICATION.md &amp;&amp; grep -q "^status: passed" .planning/phases/17-cross-cutting-verification/17-VERIFICATION.md &amp;&amp; grep -q "27e5af3" .planning/phases/17-cross-cutting-verification/17-SUMMARY.md &amp;&amp; grep -q "c08ced5" .planning/phases/17-cross-cutting-verification/17-SUMMARY.md</automated>
  </verify>
  <done>
- Both files exist
- `17-VERIFICATION.md` frontmatter has `status: passed`
- `17-SUMMARY.md` body cites both key commits (27e5af3 suite registration, c08ced5 cache fix)
- Both files reference the migrations integration suite by path
- Both files at ≥ 50 lines (sanity check)
  </done>
</task>

<task type="auto">
  <name>Task 3: Document the multiroot mutex flake in AI_INSTRUCTIONS.md</name>
  <files>AI_INSTRUCTIONS.md</files>
  <action>
In `AI_INSTRUCTIONS.md`, locate the "Integration Test Structure" section (starts around line 279). Add a short callout — either as a new paragraph at the end of that section or as a small "Local-dev gotcha" sub-heading — covering the multiroot integration suite mutex flake.

Suggested content (executor may rephrase; keep to 4-6 lines):

> **Local-dev gotcha — VS Code mutex:** Running `npm run test:integration` while your own VS Code is open causes the Dev Host launch to fail with `Another instance of app 'Code' is already active`. This is environmental — not a test regression. Two workarounds:
> 1. Close your editor before running the integration suite, OR
> 2. Pass a separate user-data-dir to `@vscode/test-electron` (or run via CI which uses a clean profile per job).

Frame it as a contributor note, not a bug. The audit's tech-debt entry explicitly says "Not a regression. Document in contributor guide or isolate to CI." — this is the documentation step.

Do not modify any other section of `AI_INSTRUCTIONS.md`.
  </action>
  <verify>
    <automated>grep -q "Another instance of app" AI_INSTRUCTIONS.md &amp;&amp; grep -q "user-data-dir\|npm run test:integration" AI_INSTRUCTIONS.md</automated>
  </verify>
  <done>
- `AI_INSTRUCTIONS.md` carries the mutex-flake callout in the Integration Test Structure region
- Grep confirms `Another instance of app` literal is present
- The callout names `npm run test:integration` and at least one workaround (close editor / separate user-data-dir)
  </done>
</task>

<task type="auto">
  <name>Task 4: Add v1.4.0 cache-invalidation follow-up entry to STATE.md</name>
  <files>.planning/STATE.md</files>
  <action>
Append a new section to `.planning/STATE.md` (or extend an existing learnings/follow-up section if one is present) capturing the v1.4.0 cache-invalidation tech debt so it survives milestone closure.

Suggested content (executor may rephrase; ~6-10 lines):

```markdown
## v1.4.0 Carry-Forward Tech Debt

> Closed by Phase 18 audit-rollup; pattern remains for future redesign.

**`activeProjectCache` invalidation pattern (src/common.ts hasFeaturesFolder):**

The Phase 12 active-project block re-reads `discoveryDepth` at lookup time
rather than invalidating `activeProjectCache` when discovery-influencing
settings change. Working but ad-hoc — see commit `c08ced5` and the WHY
comment near `src/common.ts:347`. Recommended follow-up: pair
`clearScanResultCache()` with project-list invalidation when settings
change. Tracked here so the v1.4.0 milestone-audit recommendation isn't lost.

**Multiroot integration mutex flake:** environmental — documented in
`AI_INSTRUCTIONS.md` § "Integration Test Structure". No code action needed.
```

If `.planning/STATE.md` already has a "Learnings" or "Carry-Forward" section, extend it instead of creating a new one.

Do not modify the YAML frontmatter or the existing "Project State / Current Position / Performance Metrics" content.
  </action>
  <verify>
    <automated>grep -q "activeProjectCache" .planning/STATE.md &amp;&amp; grep -q "c08ced5\|clearScanResultCache" .planning/STATE.md</automated>
  </verify>
  <done>
- `.planning/STATE.md` carries an entry mentioning `activeProjectCache` and either commit `c08ced5` or `clearScanResultCache`
- Existing frontmatter and core State sections untouched
  </done>
</task>

</tasks>

<out_of_scope>
- **`package.json` version bump 1.3.0 → 1.4.0**: explicitly deferred to `/gsd-complete-milestone v1.4.0` per `.planning/v1.4.0-MILESTONE-AUDIT.md` ("Bump to 1.4.0 is part of /gsd-complete-milestone"). Do NOT bump in this plan.
- **Re-running phase verifiers** (`/gsd-validate-phase 15|16|17`): all three already carry `nyquist_compliant: true` and `status: complete` in their VALIDATION.md (audit confirmed 2026-05-04). No re-validation needed.
- **Code-level redesign of `activeProjectCache`**: the audit explicitly says "working but ad-hoc"; the planner spec says "do NOT redesign". Documented as carry-forward debt only (Task 4).
- **New tests**: this is doc/cleanup; no new tests. The 696 unit suite + migrations integration suite stay green by construction (no source code touched).
</out_of_scope>

<verification>
This plan touches no source code, so no lint or test runs are required after the doc edits. Per-task `<automated>` greps are sufficient.

End-of-plan sanity:
- `ls .planning/phases/16-deprecate-featurespath/16-SUMMARY.md .planning/phases/17-cross-cutting-verification/17-SUMMARY.md .planning/phases/17-cross-cutting-verification/17-VERIFICATION.md` — all three exist.
- `grep -q "Another instance of app" AI_INSTRUCTIONS.md` — passes.
- `grep -q "activeProjectCache" .planning/STATE.md` — passes.
</verification>

<success_criteria>
1. Phase 16 phase-level SUMMARY exists and lists all 7 DEP-* requirements as verified.
2. Phase 17 phase-level SUMMARY and VERIFICATION exist; VERIFICATION has `status: passed` with explicit citations to commits 27e5af3 and c08ced5 plus the 15-HUMAN-UAT closeout.
3. AI_INSTRUCTIONS.md documents the multiroot mutex flake with a workaround.
4. STATE.md carries a v1.4.0 carry-forward entry naming `activeProjectCache`.
5. Zero source-code change, zero test impact.
</success_criteria>

<output>
After completion, create `.planning/phases/18-address-v1-4-0-tech-debt-artifact-rollups-mock-cleanup/18-02-SUMMARY.md` with:
- frontmatter: `phase: 18-...`, `plan: 02`, `status: complete`, `requirements-completed: []`
- short paragraph confirming all five files written, no source-code touched, audit gaps closed.
</output>
