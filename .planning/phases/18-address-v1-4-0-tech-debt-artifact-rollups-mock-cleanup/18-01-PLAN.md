---
phase: 18-address-v1-4-0-tech-debt-artifact-rollups-mock-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/unit/vscode.mock.ts
  - src/common.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "test/unit/vscode.mock.ts no longer carries the dead `suppressMultiConfigNotification` fallback (Phase 15 Finding 1 closed)"
    - "src/common.ts hasFeaturesFolder() Phase-12 active-project block carries an explanatory comment that the read-time discoveryDepth check is a deliberate ad-hoc cache-invalidation workaround"
    - "Lint stays clean and the full 696-passing unit suite stays green — zero behavior change"
  artifacts:
    - path: "test/unit/vscode.mock.ts"
      provides: "vscode workspace mock with no legacy `suppressMultiConfigNotification` branch"
      contains: "no occurrence of the literal `suppressMultiConfigNotification`"
    - path: "src/common.ts"
      provides: "hasFeaturesFolder() with documented cache-vs-discoveryDepth pattern"
      contains: "comment near L347 explaining why discoveryDepth is re-read at lookup time"
  key_links:
    - from: "test/unit/vscode.mock.ts"
      to: "Phase 15 Finding 1"
      via: "deletion of lines 171-173"
      pattern: "suppressMultiConfigNotification"
    - from: "src/common.ts (Phase 12 block)"
      to: "Phase 17 lesson recorded in 17-03-SUMMARY"
      via: "inline WHY comment above the discoveryDepth re-read"
      pattern: "discoveryDepth"
---

<objective>
Close two small code-level debt items from `.planning/v1.4.0-MILESTONE-AUDIT.md` in a single coherent commit:

1. **Phase 15 Finding 1** — remove the dead `suppressMultiConfigNotification` fallback from `test/unit/vscode.mock.ts:171-173`. Unreachable since the migration helper switched to `cfg.inspect()`.
2. **Phase 17 ad-hoc cache pattern** — add a one-or-two-line WHY comment in `src/common.ts` near the Phase 12 active-project block (around L342-L347) so future readers know the read-time `discoveryDepth` re-read is a deliberate workaround for `activeProjectCache` not being invalidated when discovery-influencing settings change.

Purpose: Closure phase for milestone v1.4.0. Audit explicitly requests both items as cosmetic cleanup; neither has any behavioral effect.

Output: Two-file edit, lint clean, full unit suite green at 696 passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.4.0-MILESTONE-AUDIT.md
@.planning/phases/15-notification-suppression/15-VERIFICATION.md
@.planning/phases/17-cross-cutting-verification/17-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Target source ranges. Executor edits exactly these blocks. -->

From `test/unit/vscode.mock.ts` lines 171-173 (DELETE):
```typescript
      if (key === 'suppressMultiConfigNotification') {
        return false;
      }
```
Surrounding context (preserve): the immediately-prior `discoveryStopOnFirstHit` branch at L168-L170 stays; the immediately-following `return defaultValue;` at L174 stays. Net delta: −3 lines, no other edits to the file.

From `src/common.ts` lines 341-348 (ADD COMMENT, no logic change):
```typescript
    // === Phase 12: Check active project from project list ===
    // Phase 17 fix: also gate on currentDiscoveryDepth so a stale activeProject
    // (cached at activation depth) does not resurrect a subdir config when the user
    // later lowers discoveryDepth below where the active project lives.
    if (!isManualProjectPathMode(folder.uri)) {
      const activeProject = getActiveProject(folder.uri);
      const currentDiscoveryDepth = vscode.workspace.getConfiguration("gs-behave-bdd", folder.uri).get<number>("discoveryDepth") ?? 3;
      if (activeProject && activeProject.depth <= currentDiscoveryDepth) {
```
The existing 3-line "Phase 17 fix" comment WHAT is fine but does not say WHY this pattern (vs. proper invalidation) was chosen. Append one or two lines explaining the audit's "ad-hoc but deliberate" disposition — see Task 2 action.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead `suppressMultiConfigNotification` fallback from vscode.mock.ts</name>
  <files>test/unit/vscode.mock.ts</files>
  <action>
Delete lines 171-173 of `test/unit/vscode.mock.ts` — the three-line `if (key === 'suppressMultiConfigNotification') { return false; }` block inside the mock `getConfiguration().get()` chain. Leave every other line untouched. Do not reflow surrounding code. Do not touch the `enum ConfigurationTarget` block or any other mock surface.

Why: Phase 15 Plan 05 removed the legacy boolean from the schema, the `WorkspaceSettings` field, and from production reads. The migration helper in `src/notifications.ts` reads via `cfg.inspect()`, never via `cfg.get()`. This fallback returns `false` to a code path that no longer exists — pure dead code per Phase 15 Finding 1 (15-VERIFICATION.md / 15-SUMMARY.md Finding 1).

After the edit, verify the literal `suppressMultiConfigNotification` no longer appears anywhere under `test/unit/` outside the two allow-listed Phase 15 test files (`notifications.test.ts`, `packageJsonSchema.test.ts`).

Then run lint and unit tests per CLAUDE.md mandate.
  </action>
  <verify>
    <automated>npx eslint src --ext ts &amp;&amp; npm run test:unit &amp;&amp; (grep -rn "suppressMultiConfigNotification" test/unit/ | grep -v -E "notifications\.test\.ts|packageJsonSchema\.test\.ts" ; [ $? -ne 0 ])</automated>
  </verify>
  <done>
- `test/unit/vscode.mock.ts` no longer contains `suppressMultiConfigNotification` (grep confirms)
- `npx eslint src --ext ts` exits 0 with no output
- `npm run test:unit` reports `696 passing` (no regressions, no new tests required)
- `grep -rn "suppressMultiConfigNotification" test/unit/` matches ONLY `notifications.test.ts` and `packageJsonSchema.test.ts` (the allow-listed Phase 15 test files)
  </done>
</task>

<task type="auto">
  <name>Task 2: Document ad-hoc cache-vs-discoveryDepth pattern in src/common.ts</name>
  <files>src/common.ts</files>
  <action>
In `src/common.ts`, locate the `// Phase 17 fix:` comment block above `if (!isManualProjectPathMode(folder.uri)) {` (around line 342). Augment the existing WHAT comment with a short WHY rationale so a future reader does not "fix" the read-time re-read by hoisting it. Keep the addition to **one or two lines**, conversational, focused on WHY (per CLAUDE.md Comments guidance: "explain the WHY, not the WHAT").

Suggested wording (executor may rephrase but must preserve intent):

```
// Phase 17 fix: also gate on currentDiscoveryDepth so a stale activeProject
// (cached at activation depth) does not resurrect a subdir config when the user
// later lowers discoveryDepth below where the active project lives.
// Note: this is a deliberate read-time check, not a cache-invalidation hook —
// activeProjectCache outlives the settings that influence its keys, and a
// proper clearScanResultCache()-paired invalidation is tracked as v1.4.0
// follow-up tech debt (see .planning/v1.4.0-MILESTONE-AUDIT.md tech_debt list).
```

Do NOT change any code — comment-only edit. Do NOT touch any other file in this task. Then run lint and unit tests.

Constraint check: the audit's tech-debt entry says the pattern is "working but ad-hoc" and the planner spec says "do NOT redesign". This task is comment-only.
  </action>
  <verify>
    <automated>npx eslint src --ext ts &amp;&amp; npm run test:unit &amp;&amp; grep -n "follow-up tech debt\|cache-invalidation hook\|deliberate read-time" src/common.ts</automated>
  </verify>
  <done>
- `src/common.ts` carries a 2-line addition to the existing "Phase 17 fix" comment block explaining WHY (cache outlives setting; proper invalidation tracked as follow-up debt)
- Zero code change — `git diff --stat src/common.ts` shows only inserted comment lines, zero deletions of executable lines
- `npx eslint src --ext ts` exits 0 with no output
- `npm run test:unit` reports `696 passing`
  </done>
</task>

</tasks>

<verification>
After both tasks land:
- Single combined check: `npx eslint src --ext ts && npm run test:unit` — both green.
- Diff sanity: `git diff --stat` should show two files modified, ~5 line net change (−3 in mock, +2 in common.ts).
- No production behavior change. The migrations integration suite (Phase 17) and all 696 unit tests must remain green.
</verification>

<success_criteria>
1. `test/unit/vscode.mock.ts` no longer references `suppressMultiConfigNotification` (Phase 15 Finding 1 closed).
2. `src/common.ts` Phase 12 block carries a WHY comment documenting the deliberate ad-hoc cache pattern (Phase 17 audit lesson).
3. Lint clean, 696 unit tests green, no regressions in any sub-suite.
4. Both edits land in a single logical commit (executor's choice — single commit per plan is the norm).
</success_criteria>

<output>
After completion, create `.planning/phases/18-address-v1-4-0-tech-debt-artifact-rollups-mock-cleanup/18-01-SUMMARY.md` with:
- frontmatter: `phase: 18-...`, `plan: 01`, `status: complete`, `requirements-completed: []`
- one short paragraph confirming both edits and the green lint + unit gate.
</output>
