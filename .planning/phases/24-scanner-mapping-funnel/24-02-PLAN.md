---
phase: 24-scanner-mapping-funnel
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/parsers/stepMappings.ts
  - test/unit/parsers/executeStepsMappings.test.ts
autonomous: true
requirements: [REFS-01, REFS-02, REFS-03, REFS-04]
must_haves:
  truths:
    - "getStepMappingsForStepsFileFunction returns execute_steps call-site mappings unioned with feature-file mappings for a given step-def function line"
    - "getStepMappings(featuresUri) NEVER includes execute_steps rows (REFS-04 regression guard)"
    - "An ambiguous-type (leading And/But/*) call step matches by trying given/when/then buckets in order, each falling back to the step bucket; no match across all → no mapping"
    - "rebuildExecuteStepsMappings deletes then rebuilds per-workspace and calls refreshStepReferencesView (NOT retriggerSemanticHighlighting)"
  artifacts:
    - path: "src/parsers/stepMappings.ts"
      provides: "parallel executeStepsMappings array, rebuild/delete/lookup/live-match functions, _getCompiledStepDefs refactor, union edit"
      exports: ["rebuildExecuteStepsMappings", "deleteExecuteStepsMappings", "getStepFileStepForExecuteStep", "matchExecuteStepsContent"]
    - path: "test/unit/parsers/executeStepsMappings.test.ts"
      provides: "rebuild/union/lookup/live-match tests + REFS-04 regression guard"
      min_lines: 100
  key_links:
    - from: "src/parsers/stepMappings.ts getStepMappingsForStepsFileFunction"
      to: "executeStepsMappings array"
      via: ".concat of same stepFileStep predicate"
      pattern: "executeStepsMappings"
    - from: "src/parsers/stepMappings.ts rebuildExecuteStepsMappings"
      to: "_getStepFileStepMatch + _getCompiledStepDefs + getExecuteStepsCallSteps"
      via: "match loop over call steps"
      pattern: "_getStepFileStepMatch"
---

<objective>
Extend `stepMappings.ts` with a parallel `executeStepsMappings` array and the funnel that unions execute_steps call sites into the single reference-lookup function every consumer already calls — delivering REFS-01/02/03 with zero consumer-side changes — while proving `getStepMappings()` stays exec-free (REFS-04).

Purpose: The union point `getStepMappingsForStepsFileFunction` (stepMappings.ts:33) is read by CodeLens, the StepReferences tree, and native Find All References. Concatenating exec mappings there makes reference counting "fall out" for free. Keeping exec rows out of the flat `stepMappings` array preserves every existing integration suite's WkspParseCounts (REFS-04 / RESEARCH Pitfall 4).

Output: parallel array + `rebuildExecuteStepsMappings`, `deleteExecuteStepsMappings`, `getStepFileStepForExecuteStep`, `matchExecuteStepsContent`, a `_getCompiledStepDefs` refactor extracted from `_getFilteredSteps`, the one-line union edit, and a unit suite including the required regression guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-scanner-mapping-funnel/24-CONTEXT.md
@.planning/phases/24-scanner-mapping-funnel/24-RESEARCH.md
@.planning/phases/24-scanner-mapping-funnel/24-PATTERNS.md
@src/parsers/executeStepsParser.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _getCompiledStepDefs refactor + parallel array + delete + union edit + REFS-04 guard</name>
  <files>src/parsers/stepMappings.ts, test/unit/parsers/executeStepsMappings.test.ts</files>
  <read_first>
    - src/parsers/stepMappings.ts (full file, 191 lines — union point :33-37, getStepMappings :40-42, deleteStepMappings :45-47, _getFilteredSteps :97-114, _getStepFileStepMatch :119-190)
    - src/parsers/executeStepsParser.ts (ExecuteStepsCallStep shape, getExecuteStepsCallSteps — imported here)
    - src/parsers/featureParser.ts lines 23-33 (FeatureFileStep — the shape ExecuteStepsCallStep is structurally assignable to)
    - test/unit/parsers/stepMappingRegexCache.test.ts (full, 139 lines — makeStepFileStep/makeFeatureFileStep/buildMaps helpers, suite structure)
    - .planning/phases/24-scanner-mapping-funnel/24-PATTERNS.md lines 159-271 (stepMappings extension pattern assignments) and 378-412 (test suite structure + regression guard)
  </read_first>
  <behavior>
    - getStepMappingsForStepsFileFunction(stepsFileUri, lineNo) returns feature-file mappings AND execute_steps mappings whose stepFileStep matches the same URI + functionDefinitionRange.start.line predicate.
    - getStepMappings(featuresUri) returns ONLY flat stepMappings rows — never any execute_steps row, even after rebuildExecuteStepsMappings has run for that featuresUri.
    - deleteExecuteStepsMappings(featuresUri) removes only that workspace's exec rows and leaves stepMappings untouched.
    - _getCompiledStepDefs(stepDefsUri) returns { exactSteps, paramsSteps, compiledExactRegexes, compiledParamsRegexes }; _getFilteredSteps produces byte-identical output to today (existing tests stay green).
  </behavior>
  <action>
    Add import `{ ExecuteStepsCallStep, getExecuteStepsCallSteps } from './executeStepsParser'`. Add module-level `let executeStepsMappings: StepMapping[] = [];` as a SEPARATE array — never push exec rows into `stepMappings` (RESEARCH Pitfall 4). The StepMapping class needs NO change; ExecuteStepsCallStep is assignable to its `featureFileStep: FeatureFileStep` param via structural typing (only .uri/.range/.fileName/.text/.textWithoutType/.stepType are read downstream — RESEARCH Pattern 3).

    Refactor `_getFilteredSteps` (:97-114) per CONTEXT: extract the wkspStepFileSteps→exactSteps/paramsSteps→compiled-regex-maps portion (lines 99-111) into a new `function _getCompiledStepDefs(stepDefsUri: vscode.Uri)` returning `{ exactSteps, paramsSteps, compiledExactRegexes, compiledParamsRegexes }`. Rewrite `_getFilteredSteps` to call `_getCompiledStepDefs(stepDefsUri)` and add `featureFileSteps` — output object identical to before.

    Edit the union point `getStepMappingsForStepsFileFunction` (:33-37): return the existing `stepMappings.filter(...)` `.concat(` the SAME predicate applied to `executeStepsMappings` `)`. This single edit delivers REFS-01/02/03.

    Add `export function deleteExecuteStepsMappings(featuresUri)` mirroring deleteStepMappings (:45-47) but filtering `executeStepsMappings`. Do NOT modify `getStepMappings` (:40-42) — it must keep filtering only the flat `stepMappings` array.

    Create test/unit/parsers/executeStepsMappings.test.ts modeled on stepMappingRegexCache.test.ts: reuse the buildMaps helper verbatim, add a local `makeExecuteStepsCallStep(stepType, textWithoutType, ...)` factory. Add the REQUIRED REFS-04 regression guard test: after rebuilding both stepMappings (via a real feature-file step) and executeStepsMappings for one featuresUri, assert `getStepMappings(featuresUri)` contains zero rows whose featureFileStep is an ExecuteStepsCallStep (assert via an instanceof/marker check, or by asserting the returned length equals only the feature-file mappings count). Add a test that _getCompiledStepDefs returns the same exactSteps/paramsSteps partition as the pre-refactor _getFilteredSteps for a known step-def set. Use per-test-unique fake URIs; call deleteExecuteStepsMappings at the top of isolation-sensitive tests (Pitfall 6).
  </action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npx eslint src --ext ts</automated>
  </verify>
  <acceptance_criteria>
    - src/parsers/stepMappings.ts contains `let executeStepsMappings: StepMapping[] = [];` and a `function _getCompiledStepDefs(`
    - getStepMappingsForStepsFileFunction body contains `.concat(` applied to `executeStepsMappings`
    - getStepMappings (stepMappings.ts:40-42) is unchanged and still filters only `stepMappings`
    - test/unit/parsers/executeStepsMappings.test.ts contains a test named to the effect of "getStepMappings excludes execute_steps rows" that passes
    - All pre-existing stepMappingRegexCache / stepMappings tests remain green (refactor is behavior-preserving)
    - `npm run test:unit` exits 0 and `npx eslint src --ext ts` exits 0
  </acceptance_criteria>
  <done>The union funnel returns exec mappings, getStepMappings stays exec-free with a passing regression guard, and the _getCompiledStepDefs refactor preserves existing behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rebuildExecuteStepsMappings + getStepFileStepForExecuteStep + matchExecuteStepsContent</name>
  <files>src/parsers/stepMappings.ts, test/unit/parsers/executeStepsMappings.test.ts</files>
  <read_first>
    - src/parsers/stepMappings.ts (state after Task 1 — rebuildStepMappings :61-94 is the shape to mirror; _getStepFileStepMatch :119-190; getStepFileStepForFeatureFileStep :26-30)
    - src/parsers/executeStepsParser.ts (getExecuteStepsCallSteps, scanExecuteSteps, ExecuteStepsCallStep.isAmbiguousType)
    - src/handlers/findStepReferencesHandler.ts (refreshStepReferencesView — already imported in stepMappings.ts:7)
    - .planning/phases/24-scanner-mapping-funnel/24-PATTERNS.md lines 217-271 (rebuild shape, ambiguous-type bucket iteration, lookup + live-match helpers)
    - .planning/phases/24-scanner-mapping-funnel/24-RESEARCH.md lines 159-171, 207-211 (And/But/* ambiguous handling; behave semantics)
  </read_first>
  <behavior>
    - rebuildExecuteStepsMappings(featuresUri): deletes existing exec rows for featuresUri, fetches call steps via getExecuteStepsCallSteps, matches each via _getStepFileStepMatch against _getCompiledStepDefs(featuresUri), pushes matched StepMapping rows to executeStepsMappings, calls refreshStepReferencesView() (NOT retriggerSemanticHighlighting), returns processed count.
    - Ambiguous-type call step (isAmbiguousType=true): try stepType "given", then "when", then "then" (each with _getStepFileStepMatch's built-in step-bucket fallback); first bucket that yields a match wins; none → no mapping (silent).
    - Non-ambiguous call step: single _getStepFileStepMatch on its resolved stepType.
    - getStepFileStepForExecuteStep(fileUri, lineNo): returns the matched StepFileStep for the exec call step at that file+line, or undefined.
    - matchExecuteStepsContent(featuresUri, content): live-text matching for Phase 25 — scanExecuteSteps(content) then match each result through _getStepFileStepMatch using _getCompiledStepDefs(featuresUri); returns match results WITHOUT persisting to executeStepsMappings.
  </behavior>
  <action>
    Add `export function rebuildExecuteStepsMappings(featuresUri: vscode.Uri): number` mirroring rebuildStepMappings (:61-94): performance.now() timing, call deleteExecuteStepsMappings(featuresUri) first, get compiled step defs via `_getCompiledStepDefs(featuresUri)`, loop `getExecuteStepsCallSteps(featuresUri)`. For each call step: if `isAmbiguousType`, iterate buckets ["given","when","then"] — build a shallow candidate with that stepType and call `_getStepFileStepMatch(candidate, exactSteps, paramsSteps, compiledExactRegexes, compiledParamsRegexes)`; take the first non-null; else call the matcher once with the resolved stepType. On match push `new StepMapping(featuresUri, stepFileStep, callStep)` to executeStepsMappings. Call `refreshStepReferencesView()` (do NOT call retriggerSemanticHighlighting — exec sites are in .py, no gherkin highlighting, per CONTEXT). End with a diagLog timing line following the rebuildStepMappings :90-91 format. Return processed count.

    Add `export function getStepFileStepForExecuteStep(fileUri: vscode.Uri, lineNo: number): StepFileStep | undefined` mirroring getStepFileStepForFeatureFileStep (:26-30) but reading executeStepsMappings and matching via `urisMatch(sm.featureFileStep.uri, fileUri)` (never `===` on URIs — AI_INSTRUCTIONS.md rule) and `.range.start.line === lineNo`.

    Add `export function matchExecuteStepsContent(featuresUri: vscode.Uri, content: string)` — call scanExecuteSteps(content, ...), then for each call step run the same ambiguous-vs-single matching path against `_getCompiledStepDefs(featuresUri)`, returning the call-step→StepFileStep pairings (shape at discretion; used by Phase 25 for live diagnostics). Do NOT push to executeStepsMappings and do NOT touch the cache — this is a pure live-text path.

    Add tests to executeStepsMappings.test.ts: rebuildExecuteStepsMappings produces the expected number of mappings for a fixture of call steps + step defs; the union in getStepMappingsForStepsFileFunction returns those rows for the step-def line; ambiguous leading-And matches when a def exists for exactly one of given/when/then; getStepFileStepForExecuteStep returns the right def and undefined off-line; matchExecuteStepsContent returns matches without mutating executeStepsMappings (assert getStepMappingsForStepsFileFunction is unchanged after a matchExecuteStepsContent call). Per-workspace-not-per-root: rebuild once with a featuresUri, assert counts do not double (Pitfall 3 — unit-level via a single rebuild call).
  </action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npx eslint src --ext ts</automated>
  </verify>
  <acceptance_criteria>
    - src/parsers/stepMappings.ts exports `rebuildExecuteStepsMappings`, `getStepFileStepForExecuteStep`, `matchExecuteStepsContent`
    - rebuildExecuteStepsMappings calls `refreshStepReferencesView` and does NOT call `retriggerSemanticHighlighting`
    - A test asserts a leading-And ambiguous call step matches a given/when/then-only step definition
    - A test asserts matchExecuteStepsContent does not change getStepMappingsForStepsFileFunction output (no persistence)
    - `npm run test:unit` exits 0 and `npx eslint src --ext ts` exits 0
  </acceptance_criteria>
  <done>Execute_steps call sites rebuild into the parallel array, surface through the union point, and expose the per-line lookup and live-text match helpers Phases 25/26 need — all with matching-engine reuse and no gherkin-highlighting side effect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached call steps → matching engine | In-memory ExecuteStepsCallStep records run through existing regex matcher; no execution of scanned content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Information disclosure (count leakage into integration assertions) | executeStepsMappings vs flat stepMappings | mitigate | Keep exec rows in a separate array; never push to stepMappings; regression guard asserts getStepMappings excludes exec rows (REFS-04) |
| T-24-04 | Denial of Service (ReDoS) | compiled step-def regexes reused via _getStepFileStepMatch | accept | Reuses the existing, already-shipped matcher and pre-compiled regexes unchanged — no new regex construction on the match path |
| T-24-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No new packages in this phase |
</threat_model>

<verification>
- `npm run test:unit` green including the REFS-04 regression guard
- `npx eslint src --ext ts` clean
- getStepMappings(featuresUri) returns zero execute_steps rows after a full rebuild
</verification>

<success_criteria>
The union point returns execute_steps mappings for CodeLens/tree/native-refs consumers with no consumer changes, getStepMappings stays exec-free (REFS-04), ambiguous-type steps resolve across buckets, and the live-match + per-line lookup helpers exist — full suite and eslint green.
</success_criteria>

<output>
Create `.planning/phases/24-scanner-mapping-funnel/24-02-SUMMARY.md` when done
</output>
