---
phase: 24-scanner-mapping-funnel
plan: 03
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - src/parsers/fileParser.ts
autonomous: true
requirements: [REFS-01, REFS-02, REFS-03, REFS-04]
must_haves:
  truths:
    - "Every .py in allPyFiles (steps files, helper modules, environment.py) is scanned for execute_steps call sites during the initial full parse, independent of the behave subprocess result"
    - "rebuildExecuteStepsMappings(wkspSettings.featuresUri) runs exactly once per workspace, after the per-root rebuildStepMappings loop, in both the initial-parse and debounced-reparse paths"
    - "The debounced python reparse rescans the edited .py (any watched .py, not just isStepsFile) before rebuilding exec mappings and firing onStepMappingsRebuilt"
    - "A workspace with zero execute_steps calls shows unchanged WkspParseCounts and the full existing unit suite stays green (REFS-04)"
  artifacts:
    - path: "src/parsers/fileParser.ts"
      provides: "allPyFiles exec scan in _parseStepsFiles + rebuildExecuteStepsMappings insertion after both rebuild loops + debounced .py rescan"
      contains: "rebuildExecuteStepsMappings"
  key_links:
    - from: "src/parsers/fileParser.ts _parseStepsFiles"
      to: "parseExecuteStepsFileContent (executeStepsParser)"
      via: "loop over allPyFiles reading content"
      pattern: "parseExecuteStepsFileContent"
    - from: "src/parsers/fileParser.ts rebuild sites (~:610, ~:811)"
      to: "rebuildExecuteStepsMappings (stepMappings)"
      via: "single per-workspace call after the per-root loop, before onStepMappingsRebuilt"
      pattern: "rebuildExecuteStepsMappings"
---

<objective>
Wire the scanner and mapping funnel into `fileParser.ts`'s parse cycle: scan all watched `.py` files during the initial parse, rescan the edited `.py` in the debounced path, and call `rebuildExecuteStepsMappings` exactly once per workspace after the existing per-root rebuild loops (before `onStepMappingsRebuilt` fires) — proving zero behavior change when no execute_steps calls exist (REFS-04).

Purpose: This is the orchestration that makes real reference counts appear in the live editor. `fileParser.ts` owns debounce timing and rebuild sequencing; the new work is inserted at existing insertion points, never a new orchestrator. Correct per-workspace (not per-root) placement is the key to avoiding duplicated counts in multi-path workspaces (RESEARCH Pitfall 3).

Output: three surgical edits to `fileParser.ts` — the allPyFiles exec scan in `_parseStepsFiles`, and the `rebuildExecuteStepsMappings` insertion in each of the two rebuild paths (with a preceding single-file rescan in the debounced path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-scanner-mapping-funnel/24-CONTEXT.md
@.planning/phases/24-scanner-mapping-funnel/24-RESEARCH.md
@.planning/phases/24-scanner-mapping-funnel/24-PATTERNS.md
@src/parsers/executeStepsParser.ts
@src/parsers/stepMappings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan allPyFiles in _parseStepsFiles + rebuild after the initial-parse loop</name>
  <files>src/parsers/fileParser.ts</files>
  <read_first>
    - src/parsers/fileParser.ts lines 1-15 (imports — add executeStepsParser + stepMappings exec fns), 201-309 (_parseStepsFiles: allPyFiles built :206-217, stepFiles filter :220, behave try block :224), 594-622 (initial-parse rebuild loop :607-612)
    - src/common.ts line 666 (getContentFromFilesystem — already imported in fileParser.ts:8) and 860-862 (getLines)
    - src/parsers/executeStepsParser.ts (parseExecuteStepsFileContent, deleteExecuteStepsCallSteps signatures)
    - src/parsers/stepMappings.ts (rebuildExecuteStepsMappings, deleteExecuteStepsMappings signatures)
    - .planning/phases/24-scanner-mapping-funnel/24-PATTERNS.md lines 275-323 (fileParser insertion points, verbatim current state)
    - .planning/phases/24-scanner-mapping-funnel/24-RESEARCH.md lines 213-217, 302-312 (Pitfall 3 per-workspace placement; insertion anchors)
  </read_first>
  <action>
    Add imports to fileParser.ts: `parseExecuteStepsFileContent` and `deleteExecuteStepsCallSteps` from './executeStepsParser', and `rebuildExecuteStepsMappings` (plus `deleteExecuteStepsMappings` if needed for the pre-scan clear) from './stepMappings'.

    In `_parseStepsFiles` (:201), AFTER allPyFiles is built and deduped (:217) and INDEPENDENT of the behave subprocess try/catch below (:224 — the exec scan is pure TS and must run even if behave loading fails), clear this workspace's cached exec call steps once via `deleteExecuteStepsCallSteps(wkspSettings.featuresUri)`, then loop over ALL of `allPyFiles` (NOT just `stepFiles` — CONTEXT: "anywhere it would be valid to call the method", so helper modules and environment.py are included). For each file, read content with `getContentFromFilesystem(uri)` (already imported) and call `parseExecuteStepsFileContent(wkspSettings.featuresUri, content, uri, caller)`. Add a diagLog line following the `${caller}: ...` convention reporting the number of files scanned / call sites found. Honor the existing cancelToken checks — do not scan after cancellation.

    In the initial-parse rebuild path (:607-612), insert `rebuildExecuteStepsMappings(wkspSettings.featuresUri);` immediately AFTER the `for (const root of wkspSettings.featuresUris)` loop closes at :610 — once per workspace, NOT inside the loop (RESEARCH Pitfall 3). Leave the existing `rebuildStepMappings` loop, `WkspParseCounts`, and diagLog lines untouched (REFS-04 zero-behavior-change).
  </action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npx eslint src --ext ts</automated>
    <automated>npx tsc -p test/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/parsers/fileParser.ts imports `parseExecuteStepsFileContent` and `rebuildExecuteStepsMappings`
    - `_parseStepsFiles` loops over `allPyFiles` (not `stepFiles`) calling `parseExecuteStepsFileContent`, positioned so it runs regardless of the behave try/catch outcome
    - `rebuildExecuteStepsMappings(wkspSettings.featuresUri)` appears exactly once immediately after the initial-parse `for (const root of wkspSettings.featuresUris)` loop, NOT inside it
    - The existing `for (const root ...) rebuildStepMappings(...)` loop and the `WkspParseCounts`/`getStepMappings` feeding code are unchanged
    - `npm run test:unit` exits 0 (full existing suite green = REFS-04 proof) and `npx eslint src --ext ts` exits 0
  </acceptance_criteria>
  <done>All watched .py files are scanned during the initial parse and exec mappings rebuild once per workspace, with the existing full unit suite unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: Debounced python reparse — rescan edited .py + rebuild before onStepMappingsRebuilt</name>
  <files>src/parsers/fileParser.ts</files>
  <read_first>
    - src/parsers/fileParser.ts lines 684-718 (reparseFile: python vs feature branch, content available at :692), 721-819 (_debouncePythonReparse: subprocess reload try/catch, rebuild loop :809-811, onStepMappingsRebuilt :812)
    - src/parsers/executeStepsParser.ts (parseExecuteStepsFileContent)
    - src/parsers/stepMappings.ts (rebuildExecuteStepsMappings)
    - .planning/phases/24-scanner-mapping-funnel/24-PATTERNS.md lines 314-323 (debounced insertion point, verbatim current state)
    - .planning/phases/24-scanner-mapping-funnel/24-CONTEXT.md lines 36-38 (debounced reparse rescans any watched .py, not just isStepsFile)
  </read_first>
  <action>
    In the debounced python reparse path (`_debouncePythonReparse`, timer body ~:732 onward), the edited file is `fileUri` with its `content` already threaded through (reparseFile :692 fetches it when absent). BEFORE the rebuild loop at :809, rescan the edited file's exec call sites for ANY watched `.py` (not just `isStepsFile` — CONTEXT: helper modules / environment.py count) by calling `parseExecuteStepsFileContent(wkspSettings.featuresUri, <content>, fileUri, "[reparseFile]")`. If content is not in scope inside the timer closure, read it with `getContentFromFilesystem(fileUri)` inside the timer. This refreshes only the one edited file's cache entries (parseExecuteStepsFileContent already clears that fileUri's prior entries per its per-file-clear contract).

    Then insert `rebuildExecuteStepsMappings(wkspSettings.featuresUri);` BETWEEN the `for (const root of wkspSettings.featuresUris) { rebuildStepMappings(...) }` loop (:809-811) and the `this.onStepMappingsRebuilt?.(wkspSettings.featuresUri);` call (:812) — once per workspace, after the loop, before the notify. Leave the existing rebuild loop and onStepMappingsRebuilt untouched otherwise.

    Do NOT touch the feature-file reparse branch (:695-712) — CONTEXT: feature-file reparse branch untouched, WkspParseCounts untouched.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npx eslint src --ext ts</automated>
    <automated>npx tsc -p test/tsconfig.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - In `_debouncePythonReparse`, `parseExecuteStepsFileContent(...)` is called for the edited `fileUri` before the rebuild loop, gated only on it being a watched `.py` (not on `isStepsFile`)
    - `rebuildExecuteStepsMappings(wkspSettings.featuresUri)` appears between the debounced `for (const root ...) rebuildStepMappings(...)` loop (:809-811) and `this.onStepMappingsRebuilt?.(...)` (:812)
    - The feature-file reparse branch (reparseFile :695-712) is unchanged
    - `npm run test:unit` exits 0 and `npx eslint src --ext ts` exits 0
  </acceptance_criteria>
  <done>Editing any watched .py refreshes its exec call sites and rebuilds exec mappings once per workspace before consumers are notified; the feature-file path and existing counts are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem .py content → parseExecuteStepsFileContent | Content read via existing getContentFromFilesystem, fed to the scanner; no execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Denial of Service (scan latency in debounce) | allPyFiles exec scan + debounced rescan | mitigate | Initial scan is a pure in-memory regex/line pass reusing already-loaded content; debounced path rescans only the single edited file, not the whole set — bounded work inside the existing 500ms debounce |
| T-24-06 | Repudiation/regression (silent count drift) | rebuild placement relative to per-root loop | mitigate | rebuildExecuteStepsMappings called once per workspace after the loop; existing rebuildStepMappings loop and WkspParseCounts untouched; full existing suite green enforces REFS-04 |
| T-24-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No new packages in this phase |
</threat_model>

<verification>
- `npm run test:unit` green (full pre-existing suite unchanged = REFS-04 zero-behavior-change proof)
- `npx eslint src --ext ts` clean
- `npx tsc -p test/tsconfig.json --noEmit` clean
- rebuildExecuteStepsMappings appears once per workspace in both parse paths, after the per-root loop, before onStepMappingsRebuilt
</verification>

<success_criteria>
The parse cycle scans all watched .py files, rescans edited .py on debounce, and rebuilds exec mappings once per workspace at both insertion points — making live CodeLens counts / references tree / native refs include execute_steps call sites (REFS-01/02/03) while the full existing test suite stays green (REFS-04).
</success_criteria>

<output>
Create `.planning/phases/24-scanner-mapping-funnel/24-03-SUMMARY.md` when done
</output>
