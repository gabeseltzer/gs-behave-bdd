Feature: Settings Multi Path Primary

   Scenario: settings primary test one
      Given we have behave installed
      When we implement a successful test
      Then we will see the result

   Scenario: settings primary test two
      Given we have behave installed
      When we implement a successful test
      Then we will see the result
